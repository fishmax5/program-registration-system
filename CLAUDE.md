# CLAUDE.md — working in this repo

A Google Apps Script project bound to one Google Sheet. It reads Google
Calendars, generates and maintains a Google Form per program group, imports
responses into the workbook, orders lunch from them, and serves two web pages
to a tablet at the door. There is no framework, no build step, and no
`node_modules`: the deliverable is the `.gs` source itself.

## The one thing to know before editing

**Every `.gs` file at the root is ONE Apps Script project sharing ONE global
scope.** The numeric prefixes ask the project to evaluate them in filename
order, and `tests/helpers/source.js` reproduces that order — but nothing
*enforces* it. Apps Script evaluates files in whatever order the project has
them stored, and a GitHub-sync browser extension that writes files back
most-recently-edited-first will happily evaluate `03_sheets_and_headers`
before `02_palette_and_tags`.

That used to be fatal, and was: a top-level `const` computed from another
file's constant threw `ReferenceError: PALETTE is not defined` on open, for
every user, with a dialog nobody could dismiss their way out of. There were 34
such cross-file reads across 15 constants.

**That is fixed, and the fix is the rule to follow.** Any constant whose
initializer mentions a constant from a *different file* is declared through
`defineLazyGlobal_` (see `01a_lazy_globals.gs`) instead of `const`: the name
binds at load, the value is computed on first read — by which point every file
has evaluated, whatever order they came in.

- **Load order no longer matters.** Renumbering a file, or moving a top-level
  `const` to a later-sorting file, is safe. Keep the prefixes anyway: they are
  how a reader finds things, and how the tables below are organized.
- A constant whose initializer is self-contained (`PALETTE`, `SHEET_NAMES`,
  `EVENT_TYPES`) stays an ordinary `const`. Nothing it needs can be missing.
- **A top-level statement that *reads* a lazy global is the same hazard
  wearing a different hat.** `PROGRAM_FORM_TYPES.forEach(...)` at file scope
  forces the getter at load time; fold that work into the factory instead.
- `tests/load_order.test.js` is what holds the line. It loads the whole project
  in reverse filename order and in 60 shuffles, and checks every derived global
  reads back identically. A failure there is not a broken test — it is a new
  eager cross-file read. Wrap it in `defineLazyGlobal_`.
- Function *declarations* are hoisted across the whole project, so a function
  may freely call one defined in any other file — `defineLazyGlobal_` included.

## Where things are

Line counts are a rough guide to what you are about to load.

### Configuration and shared vocabulary (00–11)

| File | | What is in it |
|---|--:|---|
| `00_overview.gs` | 652 | Nothing but the project's header comment: every tab, the sync flow, the first-run path. **Read this first** — it is the cheapest orientation in the repo. |
| `01_logging_and_access.gs` | 276 | `log()`, the admin-only gate — which applies to `ADMIN_GATED_ACTIONS` and nothing else, so a repair or a read is open to whoever can open the workbook — and the "are you sure?" prompts. |
| `01a_lazy_globals.gs` | 63 | `defineLazyGlobal_` — the one helper that makes file load order stop mattering. Read the banner before adding a constant derived from another file's. |
| `02_palette_and_tags.gs` | 1013 | `PALETTE` and every color derived from it; `EVENT_TYPES`; the bracket tags (`Shared`, `Club`, `No Registration`, `Personalized Assistance`) and the regexes that recognize them in a calendar title. Plus the one tag that describes a DATE rather than a program — `Waitlist Only`, which closes a single session to new Active registrations whatever its capacity says — and the two lists that keep the difference straight: `PROGRAM_FLAG_COLUMNS` (ticked onto every row of a program) and `SESSION_FLAG_COLUMNS` (ticked onto the row you clicked, and nowhere else). |
| `03_sheets_and_headers.gs` | 1176 | `SHEET_NAMES`, `HEADERS` (the column list for every tab), legacy renames and header aliases, per-tab staff-owned column lists. **The schema.** `Program_Settings`' guest-list tick is `Add_Guest_To_Calendar` — it was `Add_To_Calendar`, which read as though unticking it would take the program off the calendar; it never meant that (the event is the calendar's) and the old spelling is carried across by `LEGACY_HEADER_ALIASES`. `Program_Settings` is the one tab here that was made by MERGING two: `Program_Options` and `Registrant_Notifications` had one grain, one key and one refresh between them, so they are one tab and one `PROGRAM_SETTINGS_STAFF_COLUMNS` list now — carried across by a `LEGACY_SHEET_RENAMES` entry for the first and a migration for the second.Member_Roll's household and name columns are described here; what they MEAN is `77`. Note the one place a tab's name and its schema key deliberately disagree: the session tab is `All_Program_Sessions` (renamed from `All_Program_Sessions` in September 2026, carried across in place by `LEGACY_SHEET_RENAMES`) but its header list is still keyed `HEADERS.All_Program_Sessions` — a schema key is read by code, a tab name by people. Same reasoning as `LEADER_SHEET_REGISTRY_PROP_KEY` still being spelled `INSTRUCTOR_SHEET_REGISTRY_V1`.<br><br>`All_Registrants` gained `Registration_ID` at the END of its list, which is the registration ledger's fold key written onto the row (`99k`, `99o`) — added last because a canonical column the sheet does not have yet projects to `-1` and reads back blank (`buildHeaderProjectionFromRow`, `34`), so adding one to a `HEADERS` array is safe on a tab already holding data and the next render writes it out. |
| `04_settings_and_config.gs` | 672 | `CONFIG_LAYOUT` and the settings on the Config tab — meal buffers, order-ahead days, catering policy, link display, calendar invites, automation on/off, and the Admin Notification Emails table (`ADMIN_NOTIFICATION_CATEGORIES`: who in the office is copied on which outbound mail, replacing the retired single Admin Notification / Archive Copy cells) — plus locations, addresses, and the forms Drive folder. Also the registration horizon, which is now TWO cells and one answer: `Months_Ahead` (`REGISTRATION_HORIZON_MONTH_OPTIONS`, three months by default) is how far ahead registration is open, counted from today and resolved on every read so it cannot go stale, rounded to the END of the month it lands on so it moves once a month rather than every morning — adding a month is one step up that dropdown. `Registration_Open_Through` beside it is then only the DISPLAY of what that works out to (`refreshRegistrationHorizonDisplay`, written by the calendar sync when it moves); a blank `Months_Ahead` falls back to that cell as a typed date, which is what every workbook did before this existed, and typing a date clears the rolling half rather than being overwritten by the next sync. Also `Recognize Short Series` (`Group_Series_Up_To`), the one number deciding how long a bounded calendar recurrence may be and still be read as a series rather than a standing program — see `95`; `0` turns that recognition off. Also `Sync_Minutes_Per_Run` (`SYNC_BUDGET`), how long ONE run of the registration sync may work before it stops tidily and hands the rest on — a few minutes short of whatever ceiling the account has, 25 by default for the thirty-minute Workspace one; see `98`. Also the SECOND kill switch, which answers a different question from the first: `Pause_Outbound_Mail` (`OUTBOUND_MAIL_PAUSE_OPTIONS`) stops everything that leaves the organization while the syncs, dashboards and repairs carry on — and DISCARDS what it holds rather than queueing it, because the run somebody pauses for is the run that would otherwise mail a leader about a dozen registrations that never changed. Fails open like `Automation_Enabled`: only the literal "Yes" pauses. |
| `05_form_template.gs` | 1183 | `TEMPLATE_VERSION` and the shape of the generated Google Form: item titles, page titles, the roster grid, attendance-mode choices, guests, the meal totals, and the page navigation helpers every form-shaping path writes through (`setNavigationAfterPage`). **Bump `TEMPLATE_VERSION` when the form's structure changes — page navigation counts.** |
| `06_registries_and_locks.gs` | 152 | The groupKey→Form_ID registry in Script Properties, the all-dates registry, template-version tracking, and the script locks. |
| `07_dates_and_labels.gs` | 740 | `TIMEZONE`, date formatting, and the capacity/meal hints a form's date label carries. |
| `08_execution_caches.gs` | 542 | The per-execution memo caches the sync hot paths use, and their invalidation — including `openFormCached()`, the one `FormApp.openById()` per form per run, and `openSpreadsheetCached()` beside it, the one `SpreadsheetApp.openById()` per FOREIGN spreadsheet per run (a sync opened every program registrant sheet twice: once to read the leaders' ticks back in, once to write the roster out). Neither caches a FAILURE — the banner over `openFormCached()` gives the three reasons. |
| `09_lunch_schedule_lookup.gs` | 408 | Reading `Lunch_Schedule` by date × location. |
| `10_form_date_labels.gs` | 749 | Fingerprinted writes of date labels onto a live form (`applyFormDateLabels`). |
| `11_menu_items_paste.gs` | 1107 | Adding menu items to `Lunch_Schedule` — paste CSV, in the sheet or a dialog. |

### Building the workbook (12–15)

| File | | What is in it |
|---|--:|---|
| `12_sheet_setup.gs` | 213 | `initSheet` and the `Lunch_Schedule` tab's own setup. |
| `13_lunch_only_signup_form.gs` | 1064 | The lunch-only sign-up form — the one form built from `Lunch_Schedule` rather than the calendar. Also the shared sheet-shaping helpers it grew around: `autosizeColumns`, and `ensureSheetColumns` (a layout that outgrows a tab's column count is a throw, not a resize). |
| `14_saved_column_widths.gs` | 463 | "This column should be this wide, always." |
| `15_config_sheet.gs` | 1473 | Drawing and validating the Config tab, reading its settings back (`getAdminNotificationRows` / `adminEmailsForCategory` / `isOutboundMailPaused` among them), and the two ways the office is told anything: `notifyAdmin` / `notifyAdminCategory` / `flushAdminDigest`, which now FILE a line for the daily digest (`88`) rather than sending, and `notifyAdminUrgent`, which is the old send-at-once behavior kept for the two faults that cannot wait. Neither is pausable. |

### Menus, triggers, and edits (16–18)

| File | | What is in it |
|---|--:|---|
| `16_menu_and_triggers.gs` | 814 | `onOpen`, the menu tree (the Admin submenu is now attached for everyone — the gate lives inside its destructive items, not on the menu), trigger installation. The items pressed ONCE on a workbook upgraded from an older version — the three backfills (the sign-in sheet links, the generated files, and the registration ledger's own, `99o`), the guest sweep, and the first-run import — are collected behind Admin → **One-Time Jobs**, which is what lets the sibling submenu beside it be called `Reports` and mean it. Three weekly jobs (Add Registrants in Bulk, Log Volunteer Hours, Build a Form Question) sit at the top level beside Quick Mark and Update Everything Now. **Every item names `menuFn_('action')`**, i.e. the `menu_<action>` wrapper in `99r` that counts the click, never the action itself. A new item needs its wrapper there, and `tests/menu_usage.test.js` fails until it has one. |
| `17_trigger_attribution.gs` | 371 | "Who is actually firing this handler?" — duplicate-account detection and trigger status. |
| `18_edit_handlers.gs` | 2457 | `onEdit` and everything downstream: dashboard edits, program-flag edits and how they spread to sibling rows and back onto the calendar description, the per-session `Waitlist_Only` tick and the one calendar event it is stamped onto instead, Config edits, Registrants edits, catering recount, and the Member_Roll edits that mean something (`handleMemberRollEdit`: a `Display_Name` correction carried across every tab, a `Household_Override` recomputed). Plus `readEditedBlock` — one `getValues()` of the rows an edit touched, shared by the four handlers that used to ask the sheet for two or three single cells per row (a fill-down over three hundred sessions was the better part of a thousand round trips with somebody waiting on a confirmation dialog); a read that will not go through falls back to the per-cell reads it replaced. Plus `handleProgramMonthEdit` — the `Master_Program_Dashboard` Leader dropdown, the one edit anywhere that writes to `Program_Leaders` from another tab: it asks first (this decides who may read a roster), only ever ADDS a row, refuses a fill-down, and answers a cleared cell by saying that nothing was removed and where a leader actually is.<br><br>`handleRegistrantsEdit` also APPENDS what was typed to the registration ledger (`appendRegistrantEditToLedger_`, `99k`) — the writer the whole of phase 4 turns on, because once the tab is a projection a hand edit that was never appended is overwritten by the next fold, which would be a new way to lose a change introduced by the fix for losing rows. It flushes ITSELF rather than leaving the entry buffered, since `flushPersistentRegistries()` and `75`'s `finally` both touch Script Properties and this path cannot; and it is guarded whole, because a ledger that will not write must not make somebody's saved edit report itself as failed. |

### Calendar → forms (19–26)

| File | | What is in it |
|---|--:|---|
| `19_calendar_incremental_sync.gs` | 245 | `onCalendarChange` and sync tokens. |
| `20_calendar_sync.gs` | 389 | `syncCalendars` — the entry point and its shape. |
| `21_description_tag_readers.gs` | 893 | What the system reads out of a calendar event's description, and the tag inspector. |
| `22_renamed_programs.gs` | 978 | A title change that must not cost you the roster: detection and the rename map applied across every tab and ledger — NINE stores, the ninth (`renameLeaderSheetRegistryKeys`) being the program registrant sheet registry, whose absence was the quietest fault this pass could produce: the registry kept the old key and the old stored title while the session table carried the new one, so the push wrote an empty roster over a real one and the stranded-row counter, comparing against that same stale title, filed no note — a leader left holding a live link to a sheet reading "Nobody has signed up yet" with every screen in the workbook agreeing all was well. A collision (the new name already has a sheet) moves NOTHING and reports the pair instead, because overwriting that entry would leave a real file in somebody's Drive named by no registry at all, which is the one state `89`'s sharing sweep, `82`'s filing sweep and `99h`'s doctor all stop being about at once. Also `reconcileProgramFlagColumns` / `reconcileSessionFlagColumns` — the sync bringing the dashboard's tick boxes back into line with the calendar, keyed per program and per date respectively. Also `renameProgramQuestionRows` — the store whose rows are AIMED at a program rather than owned by it: a question is matched to a form by title, so a rename left every one written for that program pointing at a name nothing answers to, still ticked Active and on no form. Carried across on the rename, the way `77` carries a person's corrected spelling, rather than discovered weeks later by somebody wondering where their waiver went. |
| `23_reconcile_sessions.gs` | 1000 | Reconciling session times, assistance settings, club tags, No-Registration effects, and the registration horizon against the calendar. |
| `24_calendar_groups.gs` | 646 | `buildEventGroups` / `processCalendarGroup` — grouping events into the thing that gets one form. |
| `25_bootstrap.gs` | 561 | `bootstrapCalendars` — the sliced first import, for setups too large for one execution. |
| `26_event_descriptions.gs` | 1126 | Stripping every registration link out of an event description and writing exactly one back. |

### Responses → the workbook (27–33)

| File | | What is in it |
|---|--:|---|
| `27_registration_import.gs` | 433 | `syncRegistrations` — the entry point — and `runRegistrationImportPhase`, the half of it that cannot lose anything: every form's new responses, into rows, onto the Registrants tab. It reads the form list OFF THE PLAN rather than off the sheet, so a run that stopped part-way resumes on the forms it never reached, and it advances the sync clock only when every form in the window has been read AND the rows landed. The other half — the budget, the lock, the tail and the hand-off — is `98`.<br><br>The step that writes the tab also hands the ledger the batch `29` composed for the rows it is about to write (`takeImportLedgerEntries`, `99k`) — inside the same `sync.step`, so a composition that throws stops the write, the clock does not move, and the responses are read again next hour. |
| `28_deletion_tombstones.gs` | 524 | Why a deleted registration stays deleted. |
| `29_form_response_processing.gs` | 778 | `processFormResponse` — one response into registrant rows, guests and meals included.<br><br>`buildRegistrantRow()` is also where the ledger's KIND is decided, so it is where the entry is COMPOSED (`composeImportLedgerEntries_`) — one `registered` always, a `waitlisted` beside it when capacity or `[Waitlist Only]` put this submission in the queue, and a `superseded` from `supersedeRegistrantRow()` naming the id that replaced it. It composes and does not append: a row built here is not a row anybody has written yet, and an entry in the ledger's own buffer would be flushed by `75`'s `finally` whether or not the render happened, so `27` hands the batch on beside the write of the rows it describes. |
| `30_registry_counts.gs` | 164 | Active / waitlist / remaining-seat counts. |
| `31_form_shape_and_migration.gs` | 396 | Is a live form still on the current template, and migrating it if not. |
| `32_dashboard_link_repair.gs` | 1853 | Every way a registration link drifts from its session, diagnosed and repaired. **A `Form_ID` naming a form that will not open is EVIDENCE, not a dead end** — which it was, and the two halves of the old resolution compounded into a repair that could not fix the commonest fault. It resolved one candidate (`registry[key] || majority(key)`), opened it, and abandoned the row if it would not open; and a workbook whose registry has no entry for a key falls through to the vote, which is the rows saying what they already say. So a whole program pointing at one DELETED form resolved to that same deleted form, failed to open, and was skipped — and `repairDashboardLinks()` then reported that every link already matched the registry. It had not looked.<br><br>Candidates are now tried in order of authority and **the first one that actually OPENS is the answer**: the registry, the same-group vote, the row's own `Edit_Form_Link`, and last what the row already says. The edit link is admissible where the view link is not, and that is not the mistake this file's banner records: an edit URL is built FROM the file id (`/d/<id>/edit`) so it NAMES a form, while a published URL carries a separate identifier no file id can be read back out of. Nothing is harvested either way — a candidate is a name, and the URLs written always come from opening that form and asking it. On a healthy workbook the first candidate opens and nothing changes. <br><br>And `describeEmptyLinkRepair()` is what a run that wrote nothing says now: **"nothing to fix" and "nothing looked at" are different answers**, reported apart because they need different next steps — every candidate gone from Drive (only a hand-typed form id can help), no registry entry so the rows could only vote for themselves (a calendar sync reseeds it), or identity columns shifted (only the calendar can rebuild them). `tests/dashboard_link_dead_form.test.js` pins the fall-through, that the healthy case is unchanged, that the published URL is never lifted off the row, and all four of those reports.<br><br>**And the case none of that may decide: TWO FORMS, ONE PROGRAM** (`showForkedFormsDialog`). Everything above assumes `Form_ID` is the truth and the links drifted — right when a column slid, and exactly wrong when a program is left holding two near-identical twins (same title, same questions, same dates) and the registrations are on the one the links open. Repairing from `Form_ID` there would rewrite the live link to point at the EMPTY twin: the dashboard would agree with itself and send every future registration somewhere nobody reads. Which one is right is a fact and it is **not on the spreadsheet** — it is the response count on each — so this reports and asks rather than repairing.<br><br>What makes it possible is the **inverse lookup** the project never had: a published URL carries its own identifier and no file id can be read out of it (which is why harvesting one is banned), but forms can be OPENED and asked for their own published URL, and the map that falls out turns a `View Live Form` link back into the form it opens. The candidate set is deliberately wider than the tab — the rows, the registry, AND `formIdsInFormsFolder_()` — because the twin is typically named by nothing at all, which is the whole shape of the fault. Choosing a form repoints that program's UPCOMING sessions through `writeFormIdOntoSessions()` (all three pointers at once, so they cannot come apart again), leaves past rows as the record of where each registration came from, and **marks the chosen form for re-import** (`99`) — because its responses were submitted while the rows named the other form and are therefore behind the sync clock. `tests/forked_forms.test.js` pins the backwards read, that agreeing rows are not reported, the response-count ordering, upcoming-only, and the mark. |
| `33_calendar_invitations.gs` | 687 | Registrants — and nobody else — as guests on the real calendar event. The office is told by mail instead: one digest per pass naming who was invited to what and how (`notifyOfficeOfCalendarInvites`), plus the one-time Admin sweep that takes staff addresses back off the events they are still on (`removeAdminGuestsFromCalendarEvents`) and its blunter sibling behind the Destructive door, `removeAllCalendarInvitesFromEvents` — every guest off every UPCOMING event and the ledger entry with them, capped and resumable, past events untouched, and it does not turn the channel off (`Add_Guest_To_Calendar` on `Program_Settings` decides that, which is why both the prompt and the summary say so). |

### Tabs the staff work in (34–42)

| File | | What is in it |
|---|--:|---|
| `34_sectioned_tables.gs` | 671 | The Upcoming/Past split every date-bearing tab uses. **The sectioned reader is used everywhere — change it carefully.** |
| `35_per_sheet_render.gs` | 133 | Per-sheet render wrappers. |
| `36_quick_mark_dialog.gs` | 1787 | Quick Mark, the sign-in desk tool on the menu — including **Add to waitlist**, the one tick that clears every mark beside it (no seat, no meal). Its writes are **optimistic**: pressing Mark draws the mark as done and clears for the next person while the sheet write runs underneath, with the walk-in question asked from the dialog's own lists first (`walkInNames`) rather than from a `needsConfirm` round trip after. Below the Mark button is **Change this registration** (`99`), which is the opposite bargain and deliberately so: it opens only on somebody already on the list, offers only the changes that person's `Program_Status` admits (`changesForStatus`), and WAITS for the answer — a cancellation shown as done and then refused is a seat given away twice. |
| `37_regular_needs.gs` | 592 | The standing notes a desk would otherwise have to memorize. |
| `38_quick_mark_index.gs` | 2175 | The cached index Quick Mark and the door pages both read, plus walk-ins and lunch-only sessions — and `applyQuickMarkLocked`, where the desk's five ticks land (the fifth, **Add to waitlist**, goes through `71`'s writer rather than setting a status here). Also `warmQuickMarkIndexIfCold` (the five-minute rebuild that keeps a stored index there to inline) and `reportOptimisticQuickMarkFailure` (a refused mark told to the office, because the desk has already moved on). Each registered person in the index carries their `Program_Status` alongside their name and slot (schema 5), which is what lets the name list say "(cancelled)" and the change panel (`99`) offer only what that row admits.<br><br>Every mark now also appends to the ledger (`appendQuickMarkCorrection_`, `99k`), reading the columns BACK off the row rather than trusting what the caller meant to write — the meal counts are added to what the row already held, so the number the desk typed is not the number the registration carries. A walk-in appends its `registered` entry BEFORE the tab is written, because a walk-in is where the absence of a second copy is worst: no form response behind the row, so `99d`'s audit cannot reconstruct it. `args.ledgerSource` is what lets the door (`74`) say it was the door, and it rides in the args `99b` stores verbatim. |
| `39_triage_sheet.gs` | 526 | Sessions the calendar stopped mentioning. Also the two things every sectioned tab's `afterWrite` hook ends with: `applyColumnVisibility` (hiding and showing in RUNS, not a column at a time) and `protectDerivedColumns`, which remembers the geometry it last built for and does nothing when it has not moved — thirteen derived columns across two zones is twenty-six warning protections at four calls each, and they were rebuilt identically on every render. The count of protections on the tab is the honesty check, and it is one call; the key is the FILE and the tab within it, because every program registrant sheet is a tab called `Sign_Up_Sheet`. |
| `40_memory_tabs.gs` | 932 | `Member_Roll` / `Program_Settings` — including `refreshProgramSettings`, the ONE pass that writes the tab `Program_Options` and `Registrant_Notifications` were merged into — now GROUPED BY KIND (`groupProgramSettingsRows` / `programSettingsGroupOf`, resolving section 13's six `PROGRAM_FORM_TYPES` off the same four controls everything else does, plus a seventh heading for the programs the calendar has stopped mentioning), the headings being real rows every reader skips (`isMemoryTabDividerValue`, which generalizes Member_Roll's retired divider) — and `seedNotificationHalf`, the newest-first rule that keeps "a row is never born blank" true through that merge. Plus the shared writer every staff-authored tab is drawn with (`writeMemoryTab`, `readSimpleTable`, the spare validation band). Also `stampMemberHouseholds` / `refreshMemberHouseholds` — where the household grouping decided in `77` meets the roll. What is particular to a roll of PEOPLE — the name split, the dedupe, retirement, the paste — is `79`, and both writers here go through its `writeMemberRollTab()`. |
| `41_club_rosters.gs` | 448 | `Club_Members`. |
| `42_legacy_tab_merge.gs` | 385 | Merging tabs from older layouts. |

### Dashboards and printed output (43–46)

| File | | What is in it |
|---|--:|---|
| `43_program_dashboard.gs` | 1060 | `renderProgramDashboard`. Still the home of the metrics block's arithmetic and every one of its column notes (`computeProgramMetrics` / `writeProgramMetricsSection`) — the block itself is now DRAWN on `Master_Program_Dashboard` (`78`), the tab whose grain it matches. |
| `44_lunch_dashboard.gs` | 1142 | `updateMasterLunchDashboard` and the catering counts. Its schedule table is drawn inside a `withRenderBatch()` scope (`97`); the hero Today strip above it deliberately is not — it is four columns wide where the scope is the table's full width. |
| `45_sign_in_sheet.gs` | 1326 | The desk's sheet for one day and one building — a **live Google Doc**, rebuilt in place so the link never goes stale, filed in `Sign-In Sheets`. Lunch on page one, everybody on page two. One row per PERSON (`signInPersonKey` / `dedupeSignInEntries`: a duplicate's meals take the MAXIMUM, a guest's ADD) — matched on the NAME alone, since the phone-number pass that used to merge two spellings sharing a name token merged every married couple instead, printing one Meiers where there were two and one meal where there were two; a shared number is reported (`collectSignInContactSuggestions`) and never folded, and two washes for how a meal is handled — yellow it leaves the building, purple it needs doing something with here. It used to export a PDF and throw the document away; `getOrCreateSignInSheetFolder()` is all that is left of that, for the backfill in `69`. |
| `46_program_leader_sheets.gs` | 1776 | **Program registrant sheets** — a live roster shared out of the workbook, banded by session. One sheet per program (not per date), so a link handed out in September is still right in March. Built on the menu, automatically a week before a program's next session for EVERY program (`ensureRegistrantSheetsForUpcomingPrograms`, capped per run), and automatically for a leader who asked to be notified whenever their program runs (`ensureProgramLeaderSheetsForNotifyingLeaders`). The identifiers still say "leader" for the same reason `LEADER_SHEET_REGISTRY_PROP_KEY` still says "instructor"; only the words a person reads changed. Two of its five leader-owned ticks reach outside the sheet: `Dropped` (a cancellation) and `Waitlisted` (a waitlisting, and the only one that can be taken back) — the second is washed peach on the row, `isLeaderSheetWaitlistedRow`.<br><br>**The most expensive thing the sync did.** One rewrite of a year of a weekly class measured 1,137 round trips against somebody else's spreadsheet, because the sheet is banded per SESSION and every band and every run of rows between them cost a handful of per-column calls — one sheet per program, hourly. The per-run loops are `getRangeList()` calls now (one per attribute, not one per attribute per session: 181, and it no longer grows with the length of the class), the whole rewrite is fingerprinted (`computeLeaderSheetFingerprint` — an hour on which nobody's roster moved costs the banner's refresh stamp and nothing else; the menu item passes `force: true`, which is the escape hatch for a sheet somebody has mangled), and both halves of the round trip share `openSpreadsheetCached()` (`08`) rather than loading the same document twice. `tests/leader_sheet_push.test.js` pins all three.<br><br>**And what a fingerprint is: a CLAIM, stored in Script Properties, about a file in somebody else's Drive.** Nothing checked that the two agreed, so when they came apart the skip was unrecoverable: the push read a fingerprint saying ten people, matched it, skipped, restamped the banner — which kept the file's modified time moving, so it looked alive — and never read the tab, which said "Nobody has signed up yet", every hour for weeks. Both halves were internally consistent; the workbook reported ten rows written and the leader reported an empty roster and neither was lying. Two changes hold the line. `leaderSheetTabReadsEmpty_()` is ONE `getValue()` on the skip path, asked only when there IS a roster to contradict, and finding the contradiction falls THROUGH to the ordinary write — so the repair is the next sync rather than somebody pressing something, and a sheet that is empty because nobody signed up still costs the refresh stamp and nothing else. It is stated over the OBSERVED tab rather than over any one cause, because there is more than one (a write that threw after `sheet.clear()`, a registry flushed by a run whose sheet write was lost, an older `createProgramLeaderSheet()` that wrote the tab and stored no fingerprint at all). And the second: **nobody writes that tab without recording what they wrote** — `createProgramLeaderSheet()`, this project's other writer of it, used to leave the entry with no fingerprint, so the registry's claim and the tab's contents were set by two code paths with no stated relationship between them. It stores the fingerprint for the rows it just wrote, after the write and never before it (an entry is still registered ahead of the fill, so a timeout mid-write leaves a findable sheet — but a fingerprint stored ahead of the write it describes would be the very pair being repaired). |

### Repair and last resorts (47–51)

| File | | What is in it |
|---|--:|---|
| `47_moving_sessions.gs` | 513 | Combine forms, or just repoint a link. |
| `48_deleting_registrations.gs` | 403 | `showDeleteRegistrationsDialog`. |
| `49_form_rebuild.gs` | 798 | Destroy and rebuild forms — the Admin-menu last resort, sliced across executions. |
| `50_deleted_form_recovery.gs` | 455 | A form that was deleted out of the Drive folder. |
| `51_form_link_doctor.gs` | 688 | One screen for every way a link goes wrong — and **it opens before it checks anything**, which is the whole shape of the file now. It used to run the entire diagnosis in `showFormLinkDoctorDialog()` and hand the result to the page, so the menu item probed every form in Drive, listed the forms folder and read every calendar in the sync window BEFORE the dialog was created. Apps Script stops an execution at its ceiling with no warning and no exception, so on a real workbook the dialog was never created: a menu item that does nothing at all and says nothing about why. The scan now lives behind `google.script.run` like every other slow thing here, on a page that is already on screen and can say "Checking…".<br><br>**Two depths, and the quick one runs on open** (`doctorScanDepth`): the sheet plus one form opened per distinct `Form_ID` to read its address, which is what decides every `wrongForm` / `staleLiveLink` finding and is what somebody opening this is nearly always here for. Drive (a probe per form, the folder listed in full) and the calendars are the deep pass, behind a button, because they cost round trips in proportion to the whole workbook rather than to the problem. Each stage is guarded on its own and a scan reports which ones it SKIPPED — a partial pass must never be drawn as a clean bill of health, and a deep pass that died leaves the quick one's findings on screen. A fix now re-reads at the depth the page is showing, because a re-read that runs out of time after the WRITE has landed reports a failure for a repair that worked. `tests/form_link_doctor_open.test.js` pins all of it.<br><br>Its first finding also got its own menu item: **🔗 Repair Dashboard Links (no calendar read)** (`repairDashboardLinks`, `32`) has always existed and was reachable only from inside this dialog or the Apps Script editor, which is no use to somebody being told to press it by name — by `99`'s notes, by the report beside them, or by anybody whose `Form_ID` column has come apart from its link. |

### Appointments and custom questions (52–55)

| File | | What is in it |
|---|--:|---|
| `52_appointments_and_slots.gs` | 450 | Slot arithmetic, appointment choice labels, booked-time reads. |
| `53_program_questions.gs` | 672 | The `Program_Questions` tab parsed into form items, and its refusals. Also `programTitleMatchKey` — a title worn down to what two spellings of ONE program have in common (no case, punctuation, spaces, bracket tag or accents), which is what `questionsForFormContext` now matches Program and Location on: a row typed "Bookclub" against a calendar saying "Book Club" was a question that appeared on NO form, with nothing on the tab to say so. It deliberately stops short of deciding two DIFFERENT titles are one — "Chair Yoga" and "Chair Yoga (Beginner)" keep different keys, because a question silently re-bound onto the wrong class is a waiver on the wrong form and neither form says so. Those are REPORTED instead (`findUnmatchedProgramQuestionRows` / `reportOrphanedProgramQuestions`, under Admin ▸ Reports, read-only and ungated like the reports beside it): the row number, what it says, and a shortlist for a person to choose from — the same bargain `32` and `85` make with a fact that is not on the spreadsheet. `tests/program_question_titles.test.js` pins the key, both halves of the matching rule, the report's three answers and the rename below. |
| `54_custom_questions.gs` | 439 | Putting those questions on a form and taking them back off — fingerprints and applied-title tracking. |
| `55_assistance_sync_and_images.gs` | 1690 | Refreshing appointment slots across forms, appointment responses, the assistance report, and form images. Also the two tabs it draws — including `applyProgramQuestionsValidation` and `refreshProgramQuestionsValidation`, the Program_Questions dropdowns split out of the render so the sync can re-offer them without rewriting the tab: that tab is redrawn only by first-run setup and the question builder, so its Program list was whatever the session table held on the day it was last drawn, and the programs missing from it were exactly the new ones somebody was writing questions for. `refreshMemoryTabs` (`40`) runs it on every pass. |

### Reviews (56–59)

| File | | What is in it |
|---|--:|---|
| `56_time_blocks.gs` | 534 | Collapsing a day of time blocks into one event. |
| `57_program_type.gs` | 144 | "What kind of program is this?" — one answer instead of four. |
| `58_program_review.gs` | 1919 | Decide programs one at a time, apply them all at once. |
| `59_appointment_review.gs` | 1559 | One month, one location, one form. |

### The door (60–64)

| File | | What is in it |
|---|--:|---|
| `60_check_in_page_server.gs` | 1226 | `doGet` and `DOOR_ROUTES` — the ordered table of every page this one deployment serves that WRITES to the workbook (cancel first, the door app last and matching everything, including a retired `?mode=walkin`/`walk-in`/`legacy` bookmark), which `checkInPageUrl` also builds its `?mode=` (and any extra `params`) from so a link and the router cannot drift. A route may also declare `frameable`, which is how ONE page — the public calendar, the only one here that writes nothing — gets `setXFrameOptionsMode(ALLOWALL)` while every page that writes keeps the default refusal. Plus `checkInRosterModeRequested`, the PIN gate, the roster read, and the mark/register handlers. The two PUBLIC pages are no longer in this table: `doGet` asks `publicEmbedRoute` (`91`) before it walks this one and `doorRouteUrlMode_` asks `publicEmbedUrlMode` before it answers, because the one thing that separates them — that another website may FRAME them — is a property of the response, not of a page. |
| `61_check_in_page_html.gs` | 1613 | `buildCheckInHtml` — the whole served page, one template literal. |
| `62_walk_in_page.gs` | 36 | **Retired (September 2026) — a banner and nothing else.** The sign-in page for people who never registered; replaced by the door app (`73`). Kept, empty, because a file here is never renumbered or removed. Its server half is `74`; `checkInRosterModeRequested` went to `60`. |
| `63_check_in_store.gs` | 798 | The door's own store: a roster it reads, a queue it writes. Its five-minute flush trigger also re-warms Quick Mark's lists when a desk write has dropped them, and flushes `99b`'s retries as their safety net. A queued mark that is given up on (`recordCheckInProblem`) is now written down TWICE: the page list it always had — capped, newest-kept, and DISMISSED by whoever next loads a roster, which on a two-site day is routinely the tablet at the other building — and a line in the office's daily digest (`88`), which is the copy that outlives that dismissal and reaches somebody whose job is to put it right. Until that second copy existed, a registration that never landed left none of the traces this project's diagnostics look for: no tombstone (nothing was deleted), nothing in the unimported-forms report (`99` — a desk mark has no form), and no mail. |
| `64_walk_in_day_store.gs` | 336 | The sign-in page's boot snapshot: today, per building, stored so the page draws it before it asks. **Deliberately not invalidated** — see its banner. |

### Program leaders (65–66)

Numbered after the door rather than beside `46_program_leader_sheets.gs`,
because renumbering an existing file is the one edit this project cannot make
(see above). Both files hold behavior only — their schema (`SHEET_NAMES`,
`HEADERS.Program_Leaders`, `PROGRAM_LEADERS_STAFF_COLUMNS`) lives in `03`, so
nothing earlier reads them at load time, and neither reads `64` at load time
either.

| File | | What is in it |
|---|--:|---|
| `65_program_leaders.gs` | 1081 | The `Program_Leaders` tab: who leads what, their addresses, their notification ticks, and `Notify_Timing` — the closed dropdown deciding WHEN a ticked leader hears from `66`: at each change, N days before a date, or on a fixed weekday before one (`parseLeaderNotifyTiming`, `leaderNotifyTimingDaysBefore`) — plus the one-time migration that carries `Program_Options`' old `Instructor_Email` column onto it, and `attachProgramLeaderRow` — the single write this tab accepts from elsewhere (`Master_Program_Dashboard`'s Leader cell), which only ever adds a row, takes the address off the leader's own other rows, and leaves the notify tick clear. Also `Title_Match`, the phrases that let a program find its leader instead of the other way round (`proposeProgramLeaderRowsFromTitles`): a program nobody has typed a row for is PROPOSED to a matching leader as a concrete row with the notify tick clear — a phrase never overrides a typed row, never shares anything and never sends anything, because `buildProgramLeaderIndex()` still reads concrete rows only. |
| `66_program_leader_notifications.gs` | 1036 | **Two channels, one tick.** Roster-change alerts: the stored per-program snapshot, the diff against it, and the one email per leader per sync that comes out of it. And the day-before digests for a leader whose `Notify_Timing` is a day count or a weekday instead: one email per session, N days ahead of it (a weekday row working its own count out per session, so one answer covers a Tuesday class and a Saturday one), listing who is on the roster — with its own ledger so an hourly pass sends it once.<br><br>**The snapshot is per LEADER, not per program** (`PROGRAM_LEADER_ROSTER_STATE_V2`, carried over from `_V1` in place): one snapshot per program was advanced only once NOBODY was still owed its changes, so a program with two leaders — one of them unreachable — kept its old snapshot and sent the leader it COULD reach the identical diff every hour. **And a hold that lasts the day stops the pass for the day**: `76`'s `retry` tells the quota (`tomorrow`) and a test-mode diversion (`diverted`) apart from quiet hours and the per-run cap (`later`), and only the first two stamp `DAY_LONG_MAIL_HOLD_PROP_KEY`. Nothing is recorded either way, so tomorrow's first sync says everything today's would have — once, instead of twenty-five held-back lines in the office digest. `tests/leader_alert_snapshot.test.js` pins both, the four holds, the carry-over, and the property the feature rests on: a repeated sync over an unchanged roster sends nothing. |

### Two months at the door (67)

| File | | What is in it |
|---|--:|---|
| `67_desk_month_sessions.gs` | 130 | `deskMonthSessions` — every session at one location from today to the end of NEXT month, grouped by day. The live read behind the day picker and the session boxes on the staff roster's register screen (`61`), and behind the club place a walk-in can take at the door. Behavior only; nothing earlier reads it at load time. |

### Migrations (68)

| File | | What is in it |
|---|--:|---|
| `68_form_state_migrations.gs` | 737 | `FORM_STATE_MIGRATIONS` — the registry of in-place repairs that carry a LIVE form from the shape it was built with to the shape the code now expects, without rebuilding it; the ledger of which have run on which form; the hourly sweep (`runFormStateMigrations`, ahead of `migrateFormsToCurrentTemplate`), which opens only the forms a pending migration is `targets`-ed at; and the Admin item that forces it now, slicing itself across executions until every form has been looked at. Behavior only, loading after everything it reads. |

### Links to the files this system makes (69)

Numbered last for the usual reason: it is behavior only, and its own
constants are the only ones it defines, so nothing earlier reads it at load
time. Its two columns live in `03` like every other schema.

| File | | What is in it |
|---|--:|---|
| `69_generated_file_links.gs` | 291 | Live links to the files this system makes outside the workbook: the sign-in sheet registry (which is also how `45` finds the document to rewrite, rather than making a second one) and its one-time backfill across both the live-Doc folder and the retired PDF one, plus the `Registrant_Sheet_Link` / `Sign_In_Sheet_Link` columns the dashboards and `All_Registrants` stamp on every render. |

### How often registrants hear from us (70)

Behavior only, and last for the usual reason: its two columns live in `03`
like every other schema, everything `33` and `40` call into it is a hoisted
function, and its own top-level `const`s stand alone.

| File | | What is in it |
|---|--:|---|
| `70_registrant_notifications.gs` | 533 | How often each program writes to the people signed up for it: the policy the calendar invites (`33`) and the reminder emails both read — resolved from `Registrant_Notifications`' tick boxes (`81`) — and the ledger that stops an hourly sync repeating a send. The appointment time a shared calendar description cannot carry is stated here. |

### Cancellation (71)

Behavior only, and numbered last so it is clear of `67`–`70`: it reads the
door pages' vocabulary and declares nothing anything else derives from.

| File | | What is in it |
|---|--:|---|
| `71_cancellation.gs` | 1063 | **One writer, three doors** — and a second writer for the waitlist beside it. `cancelRegistrantRows()` is the only place a booking becomes a cancellation — four cells, not one (`Program_Status`, `Lunch_Status`, `Manual_Override`, an `Admin_Notes` stamp), and the `Manual_Override` is what stops the next hourly sync re-deriving the row from its form response and quietly un-cancelling it. The doors: the check-in page's cancel button (`checkInCancel`), a program leader's `Dropped` tick (`applyLeaderDropsAsCancellations`, called from the import right after the leader merge), and the member's own cancel page (`buildCancelPageHtml`, served at `?mode=cancel&form=…` from the link in the calendar invite).<br><br>The waitlist half writes the same four cells for a state that is not an ending: `stampRegistrantRowWaitlisted` / `stampRegistrantRowActive`, reached from Quick Mark's **Add to waitlist** and from the leader sheet's `Waitlisted` tick (`applyLeaderWaitlistTicks`, two-way). A promotion is refused unless the place was made by hand (`wasWaitlistedByHand` — the import's capacity queue is never jumped) and the session has a free seat (`buildWaitlistSeatIndex`).<br><br>Since the ledger's phase 2, every one of those writers composes a ledger entry BEFORE it stamps and appends it only if the stamper returned true — and the append is deliberately NOT inside the stampers, because `foldRegistrationLedger()` (`99k`) calls them too and a stamper that appended would write the history it was replaying back into the ledger every time anybody folded. `CANCELLATION_LEDGER_SOURCES` is the second list of doors, in the ledger's vocabulary: `CANCELLATION_SOURCES` is written to be READ in `Admin_Notes` ("at the door" covers the check-in page and the change panel alike, which is the right detail in a sentence) while the ledger's `Source` is one value per CALL SITE and needs those two apart. |


### The door app (72–73)

One deployment, one link. The setup screen (building + day, stored on the
tablet), the A–Z name list, the personal confirm screen, and the walk-in
sign-up that used to be three separate URLs. `doGet` in `60` serves this by
default; `?mode=session` is still the staff roster. `?mode=walkin` was the
previous door page and is no longer a route — an unrecognized mode falls
through to this app, so a stale bookmark opens it rather than an error.

| File | | What is in it |
|---|--:|---|
| `72_door_app.gs` | 413 | The server half: the date-aware day read (`doorDay`), the recurring-registration writes (`applyDoorRecurring` — the rest of the month, or a club place), the either-kind contact rule, and `recordMembershipHandoff` (renamed from `sendMembershipEmail` — it never sent mail), which is now the WHOLE of what "not a member yet" does. The door used to draw the office's Membership Application on the tablet and submit it through the Forms API; that screen and its two endpoints are gone — ten minutes of address, date of birth and emergency contact is not a thing to type standing up on a shared tablet with a queue behind you, and the office would rather have a name and a number than three half-finished applications a week. What is filed instead is one line for the 10am digest (`88`) naming the person, how to reach them and the application's link — which is the only thing Config's membership form id is still read for (`membershipApplicationUrl`). |
| `73_door_app_html.gs` | 1347 | `buildDoorAppHtml` — the whole served app, one template literal, five screens redrawn into one `<main>`. **The first screen is a question asked of the PERSON — "What are you here for today?"** — every event on at that building that day, whether or not anybody has registered for it, **nothing pre-ticked** (a screen that arrives with four things ticked is a screen somebody taps past, and then everybody is signed in for everything) and **emptied again after every sign-in** (`startNextPerson`, `HERE_FOR`): it is one person's answer about one visit, never a desk filter set in the morning. Asking it first is what makes the rest short — the name list after it holds one class instead of the whole building, and the answer is already ticked on the confirm screen (`HERE_FOR` seeds `PICKED`, and they part company the moment somebody changes a tick, so going back to the list loses nothing). `Not sure? Show everyone here today` (`SHOW_ALL`) is the quiet way past it, for the person who cannot work out which class is theirs. **The name list then has two sections and a search box** — registered for what they tapped, then `Here recently`, the people who came to those same programs in the last two months and have not registered for today (`DAY.past`, from `74`), tapped into the same personal screen with their answer already on. A weekly class has the same eight people in it every week and half of them have never filled in a form. The membership application screen went with the endpoints behind it; the question is still asked on the walk-in screen, because it is the only place anybody ever asks it. |

### The door's day and its one write (74)

The server half the retired walk-in page (`62`) was built around, which
outlived it: the door app reads its day and writes its sign-ins through these,
and the boot store (`64`) builds its snapshot from the same read. Numbered at
the end for the usual reason — never renumber — and safely so: it is behavior
only, its two `const`s stand alone, everything it calls is a hoisted function,
and its schema (`SHEET_NAMES`, `HEADERS.Member_Roll`) lives in `03`.

| File | | What is in it |
|---|--:|---|
| `74_door_day_and_sign_in.gs` | 883 | `readWalkInDay` — one building, one date: the programs on, who is expected and what each is already down for, the meal, and the member roll for the search box; `walkInDay`, its PIN-gated endpoint (`doorDay` in `72` is the date-aware one the app calls); `readWalkInMembers` and its per-execution memo; and `walkInSignIn`, the one place a door sign-in becomes rows — every mark through `applyQuickMarkFromDialog`, plus `recordWalkInMember` for somebody the roll has never heard of. Also `day.past`, the regulars section of the door's name list: `collectPastRegistrantRow` keeps every row of the last `DOOR_PAST_REGISTRANT_MONTHS` months this sheet scan is walking past anyway (never a Cancelled or Superseded one — "they signed up and then cancelled" is exactly who should not be offered), and `foldPastRegistrants` matches them to TODAY'S programs by title and hands back TODAY'S session values, because a mark against August lands on the wrong session or on none. |

### The sliced-job runner (75)

Numbered after `74` (taken by the door's day-and-sign-in file above) and after
the four callers it was extracted FROM (`25`, `32`, `49`, `68`), for the usual
reason: renumbering an existing file is the one edit this project cannot make,
and every one of those five predates this file. It reads and writes the same
Script Property keys and the same state shape those four callers always did,
so a job already mid-flight when this shipped resumed on its next slice
without noticing anything changed — see the file's own banner for why that
was a design goal, not an accident.

| File | | What is in it |
|---|--:|---|
| `75_sliced_jobs.gs` | 312 | `runSlicedJob` — the state machine every multi-execution job in this project runs on: the watchdog armed before work starts, the deadline, the slice counter, stall and consecutive-error detection, and the hand-off trigger. `runSlicedItems` is the one-item-one-lock-hold inner loop the two form sweeps (`32`, `49`) share. Behavior only; the four callers supply their own state, their own budgets, and every word the person reads. |

### Every email this workbook sends (76)

Numbered after `74` (the door's day-and-sign-in file) and `75` (the sliced-job
runner) — both landed first — and for the usual reason otherwise: it is
behavior only, it defines two constants nothing else derives from, it reads no
other file's constants at load time, and `66` and `70` reach it through a
hoisted function declaration — so whatever order the project's files come in,
it is there when they call it.

| File | | What is in it |
|---|--:|---|
| `76_rationed_mailer.gs` | 401 | `sendRationedEmail` — the plumbing every send to somebody outside the workbook shares: the once-per-execution read of `MailApp.getRemainingDailyQuota()` and the floor each caller refuses to dig below, the office BCC the caller resolves from its own Config category (each address counted as its own message, because it is one), the send, the refused address remembered so the run stops paying to be told no twice, and the caller's ledger written only once the message is away. It is also the ONE place the outbound-mail pause is read (`04`): a paused message returns `'paused'` and has its `recordSent()` called anyway, so the ledger advances and the message is dropped rather than owed — the three callers in `66` and `70` treat that status as told, which is what stops the pause becoming a delayed flood. **The policies stay with their callers** — who is written to, what it says, how often, and how much of the day's hundred messages a pass may spend live in `66` and `70`. **`'held'` now means four things** — the caller's reserve, quiet hours, a message notification test mode diverted to the office, and test mode with nowhere to divert to — sharing one status on purpose (a new one would fall through every caller's branches into its "could not send" warning), so a held result carries `retry`: `later`, `tomorrow` or `diverted`. They clear at three different speeds, and a caller that read them alike turned a day-long blocker into twenty-odd identical hourly retries — each diverted one spending a real message. `dayLongMailHoldInEffect` / `recordDayLongMailHold` / `mailHoldLastsAllDay` are the shared stop `66` and `70` gate on, keyed per pass. `notifyAdmin` (`15`) deliberately does not come through here; the banner says why. Since `88`, NO caller passes a `bcc` or an `officeCopy`: the office reads one line per send in its daily digest instead, which is also why a reminder costs one message against the quota rather than one plus the size of the office table. |

### Who arrives with whom, and what they are called (77)

Behavior and vocabulary for two facts the desk used to hold in its head. Last
for the usual reason: its columns live in `03` like every other schema, its own
constants stand alone, and everything that calls into it (`29`, `36`, `38`,
`40`, `72`, `74`) reaches it through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `77_households_and_names.gs` | 453 | **A household is a guess, and a name is not a key you can retype.** `buildHouseholdAssignments` reads the people who arrive together off the contact details they share — one email, one phone — with the office's own address thrown out first (`HOUSEHOLD_INSTITUTIONAL_CONTACT_MIN`) and `Household_Override` beating the guess in both directions; `readHouseholdIndex` / `householdCompanionsOf` are how the door (`74`) and Quick Mark (`38`) ask. And the names: `parseMemberName` lifts the nickname out of `Bob (Robert)` or `Robert "Bob" Kaplan` so a desk can find somebody under what they are actually called, while `applyMemberNameCorrection` is what a staff correction in `Display_Name` costs — the spelling rewritten across every tab that carries it, plus a remembered old→new map (`canonicalMemberName`) so the responses still arriving under the wrong one are filed under the right one. |

### The program behind the sessions (78)

Numbered after `77` (taken by the households-and-names file, which landed
first) and last for the usual reason: behavior only, its own top-level constants
stand alone, and everything it reads from other files it reaches through a
hoisted function call or `HEADERS` — so it is there whatever order the files
come in. Its schema (`SHEET_NAMES.PROGRAM_MONTH`, `HEADERS.Master_Program_Dashboard`)
lives in `03` like every other tab's. The filename still says "month" for the
same reason `LEADER_SHEET_REGISTRY_PROP_KEY` still says "instructor": renaming
a file is the one edit this project cannot make.

| File | | What is in it |
|---|--:|---|
| `78_program_month_dashboard.gs` | 1672 | The `Master_Program_Dashboard` tab: **one row per PROGRAM** — one title, at the building(s) it runs in, for as long as it runs. It was one row per program-MONTH, keyed on `Form_ID`, which is the unit a form and a capacity belong to and the wrong unit for a front page: a weekly class was twelve rows a year differing only in which dates they summed. The key is now `program::<title>::<building signature>`, with `programFormLocations()` resolving the signature — `Form_ID` is what tells you a `[Shared]` program's two buildings are ONE thing to run, and is no longer the key itself, because a Regular program takes a new form every month and keying on it is how the month got into the grain. **Derived, read-only and purely additive**: nothing reads it, nothing is stored on it that is not already on a session row or on `Program_Leaders` / `Program_Settings`, and deleting the tab leaves every other behavior alone. Drawn from the session rows `renderProgramDashboard()` already holds — never a second read of that tab.<br><br>**Fifteen columns a person reads, where there were seventeen** — the rule `describeProgramMonthSchedule()` set, applied to the whole tab: the fact goes in the cell, the follow-up goes in a cell note. `Schedule` is the cadence, times, span and count (`detectProgramMonthRecurrence` names `Weekly` / `Every 2 weeks` and any week the run SKIPS — never "cancelled", because nothing here can tell a session called off from one never scheduled; the per-month breakdown is in the note). `Seats` is Registered/Max_Capacity/Fill/Waitlist as one sentence, summed over **this month and next** (`programSeatWindow` — a lifetime total only goes up, says nothing about next Tuesday, and beside a capacity reads as something bookable). `Links` is four link columns as one cell of rich text (`writeProgramMonthLinkCells`). `Leader_Source` became a yellow wash and a note. The split is **Running / Not currently running** (`partitionRunningPrograms` — a status, borrowed from `79`'s retirement section, not `34`'s date partition), running sorted by `Next_Date`, the rest by how recently they stopped. The `FIXED`-span filing problem in the old banner dissolved with the month.<br><br>**Also the home of the metrics block** — `43` still owns its arithmetic and its words (`computeProgramMetrics` / `writeProgramMetricsSection`), which is what keeps "the numbers did not move" true by construction; only the drawing came here — plus the leader-coverage line under it (`programMonthLeaderCoverage`, which reads the memoized `buildProgramLeaderIndex()`, counts the RUNNING programs, and only ever COUNTS).<br><br>**Four windows and two panes**, every one still derived: `Room` and `Notify` are read-only cells off `Program_Settings` (`programMonthSettingsCell`, through the same memoized `readNotificationPolicyRows()` the invitation and reminder passes use — a third caller of one memo, not a third read of one tab); `Notify` blank means no settings row yet and `Silent` means every box cleared. `Room` is still written on every render but HIDDEN (`PROGRAM_MONTH_HIDDEN_COLUMNS`) while nobody is filling `Room_Or_Setup` in — a column of blanks on a front page reads as an answer rather than an unasked question; unhiding it is one name off that list. The four writable ones: `Leader` (an edit writes a row on `Program_Leaders` via `handleProgramMonthEdit` in `18`) and the three PROGRAM FLAGS — `Club`, `No_Registration`, `Personalized_Assistance` — which moved here off the session table, where a program carried twelve identical copies of each. A tick writes through to every session row of that program and onto the calendar (`handleProgramMonthFlagEdit` / `applyProgramMonthFlagToSessions` in `18`, reusing the same pending-flag queue); the session table keeps the columns as hidden plumbing, because that is still where the answer is stored. Carry-forward needed no code — `leaderProgramKey()` has no month in it, and now neither does the row — and `tests/program_month.test.js` pins that rather than a mechanism. |

### The member roll (79)

Numbered after `78` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own constants stand alone, its schema
(`HEADERS.Member_Roll`, `MEMBER_ROLL_STAFF_COLUMNS`) lives in `03`, and
everything `40`, `38` and `74` reach it through is a hoisted function.

| File | | What is in it |
|---|--:|---|
| `79_member_roll.gs` | 925 | **The roll as a roll of people, not a list of strings.** `First_Name`/`Last_Name`, split out of `Display_Name` or `Name` (`splitPersonName`) wherever they are blank and the staff's once written — and **never composed back onto `Name`**, because renaming somebody belongs to `applyMemberNameCorrection()` in `77`, which does it on every tab at once; a second rename path here would leave a person's history behind under their old spelling. The dedupe that runs on every write (`mergeMemberRollRows`: additive in every column, the longer history survives, `Merged_From` is its receipt), keyed on the NAME and nothing else — it also read contact details paired with a surname and first initial, which is a description of a married couple and of everybody registered under the centre's own information address, and it folded all of them silently; a contact match is a suggestion for a person now (`memberRollContactSuggestions`), reported by Merge Duplicate Members Now — and made durable by `rememberMemberNameCorrection()`, without which the refresh rebuilds the absorbed row on the next sync. Retirement as a `Status` column and a section at the foot of the tab rather than a delete: the row keeps every note and stops being offered at the door (`38`) or in the door app's search box. And the paste-in dialog (`showMemberRollImportDialog`), column mapping and preview included, on the pattern of `11`. `writeMemberRollTab()` is the ONE writer every path reaches the tab through — `40`'s refresh and its household recompute, `74`'s door sign-up, the paste, and the menu's dedupe — which is also where `stampMemberHouseholds()` runs, after the fold rather than before it. |

## Conventions

- **Comments carry the reasoning.** This codebase explains *why* a thing is
  shaped the way it is, usually in a banner above the code and often with the
  failure it exists to prevent. Match that when you add code; do not strip it
  when you edit around it.
- **Banners.** Files open with a `// ====` banner naming their section. Keep it
  accurate if a file's contents change character.
- **Constants over literals.** Colors come from `PALETTE`, tab names from
  `SHEET_NAMES`, columns from `HEADERS`. A bare string that duplicates one of
  those is a bug waiting for a rename.
- **A change to a live shape ships its migration in the same commit.** A
  template fix reaches forms created *afterwards* and nobody else: a group's
  form is created once and reused for as long as the group runs. A version bump
  says "this is different now"; the migration is what makes it different for
  everyone already holding the old thing. So when a change moves the shape of
  something already out in the world — a form's page navigation, its questions,
  a stored registry's fields — write the state A → state B repair beside it,
  register it in `FORM_STATE_MIGRATIONS` (`68_form_state_migrations.gs`) with
  its own never-reused id, and leave the earlier entries alone: a workbook that
  has been quiet for six months runs all of them in order on its next sync. A
  migration must be **idempotent** and must return 0 when the form is already
  right — it runs hourly, and a redundant Forms write is a round trip and a new
  revision in the form's history. Give it a `targets` predicate whenever the
  change only reaches some shapes: judged from the dashboard rows, it keeps the
  sweep from opening a form the repair would write nothing to. Rebuilding the form is the fallback for a
  shape no migration recognizes, not the first answer.
- **Script Properties keys are versioned** (`..._V1`). Changing a stored
  shape means a new key, not a silent reinterpretation of the old one. The
  converse is worth knowing too: a key whose stored shape has NOT changed keeps
  its value even when the words around it are renamed —
  `LEADER_SHEET_REGISTRY_PROP_KEY` is still spelled
  `'INSTRUCTOR_SHEET_REGISTRY_V1'`, and the comment above it says why.
- **Anything served to a browser is a template literal.** Values interpolated
  into a page's inline `<script>` must be escaped — a member named O'Brien, or
  a program title containing `</script>`, otherwise ends the page mid-sentence.
  See `tests/check_in_page.test.js` for what that guards.

## Tests

Plain Node scripts, no runner, no dependencies. Each one stubs the Apps Script
services, concatenates the whole project via `tests/helpers/source.js`, and
evaluates it in a `vm` context.

```
node tests/check_in_page.test.js        # one file
for f in tests/*.test.js; do node "$f"; done
```

Two MEASUREMENTS sit beside them and assert nothing — run them before and
after a change that is about round trips:

```
node tools/sync_bench.js       # the reconcile phase and the readers after it
node tools/render_bench.js     # one rewrite of the Registrants tab
```

`tests/helpers/source.js` sorts by filename — the order the project asks for —
so a test sees the same load order a correctly-ordered deployment does.
`tests/load_order.test.js` deliberately does the opposite: it loads the project
reversed and shuffled, because a real deployment's order is not guaranteed.

All tests pass. If one starts failing on a date-bearing fixture, check whether
the fixture's month has simply gone past: `buildAppointmentChoicesForContext()`
and friends drop sessions that have already started, so a hard-coded month
expires. `tests/appointment_review.test.js` builds its month relative to today
for that reason.

## Deploying

**There is no build step. The numbered `.gs` files ARE the deliverable**, and
they go into the Apps Script project unchanged — via a GitHub-sync browser
extension in the editor, or `clasp push`.

Filenames should survive intact — the prefixes are how the project is
organized and how these tables are indexed. They are no longer *load-bearing*
(see the top of this file), so a sync tool that reorders files in the project
is no longer a load-time crash; but a renamed file still costs you every
cross-reference in this document.
Do not rename a file to fit a deployment, and do not merge files to reduce
their number.

`tools/bundle.js` still exists and still works — it writes a single
`Code.bundle.gs` for a one-off paste-install where neither of the above is
available. It is not part of the normal loop: **do not run it as a build step,
and do not treat its output as the source.** `Code.bundle.gs` is gitignored;
edits belong in the numbered files.

### The weekend, on purpose (80)

Behavior only, and numbered last for the usual reason: its two constants stand
alone, everything it calls is a hoisted function, and nothing else reads it at
load time. It was `77` on `main` until the merge that brought `77`–`79` in;
renumbering it was the one way to keep both, and the prefix is all that changed.

| File | | What is in it |
|---|--:|---|
| `80_weekend_event_loader.gs` | 328 | `showWeekendEventLoaderDialog` — the Sat/Sun dates in a window (today through the end of next month by default, with its own two date boxes) that are NOT yet on the dashboard, ticked one at a time and loaded through the sync's own `processCalendarGroup`. A weekend event on a program calendar is as often a rental or a placeholder as a program, so this is the deliberate one-off; it changes nothing about which events `syncCalendars` considers. |

### The tab those notifications are set on (81)

Behavior and one schema-free constant block, numbered last for the usual
reason: everything it calls is a hoisted function, `70` reaches it the same
way, and its own `const`s stand alone. It was `78` on `main` before the same
merge; see `80` above.

| File | | What is in it |
|---|--:|---|
| `81_registrant_notifications_tab.gs` | 441 | **The notification half of `Program_Settings`, and no longer a tab of its own.** A tick box per channel, none of them exclusive: `Add_Guest_To_Calendar` (the guest list on the real event — not whether the event is on the calendar, which this system never decides; it was `Add_To_Calendar` and the old spelling is aliased), `Week_Before`, `Day_Before`, `Morning_Of`, `Other_Reminders` (any further day counts) and `Confirm_On_Booking`; `policyFromNotificationRow` is what `70` resolves a session's policy through. What stayed here after the merge is the vocabulary and the two one-time carry-overs: `readLegacyNotifyModeRows` (the retired `Notify_Mode` / `Reminder_Days` cells) and `readLegacyRegistrantNotificationRows` / `markProgramSettingsMergeDone` (the retired tab's own ticks, translated through `LEGACY_NOTIFICATION_COLUMN_RENAMES` and lifted onto the merged row and the old tab left in the workbook marked retired rather than deleted). Both are read by `40`'s single refresh, before its write — which is the only order that works, and now the only order there is. |

### Removing one registrant (83)

Behavior only, numbered last for the usual reason: its own two constants stand
alone, its schema is `HEADERS.All_Registrants` in `03` like every other tab's,
and everything it calls — the tombstones, both renders, the two recounts — is a
hoisted function.

| File | | What is in it |
|---|--:|---|
| `83_remove_marked_registrants.gs` | 238 | **The one-row delete, as a mark and a sweep.** `48` deletes by SESSION, which is the right shape for a test run and the wrong one for "this person was entered twice" — so `Manual_Override` gained a fourth option on the registrant tabs, `REGISTRANT_REMOVE_OVERRIDE_OPTION` (`'Remove This Row'`, `02`), the one value in that column that is a request rather than a record and the one washed red (`39`). **Marking deletes nothing**: `removeMarkedRegistrants()` on the Rosters & Sharing menu is what sweeps, because a mis-click in a dropdown must not be permanent and because three duplicates found while reading down a roster are cleared in one pass. It names every row before it goes, records the tombstones (`28`) BEFORE the redraw — without which the import and both catch-ups rebuild every removed row on the next sync — redraws from the kept rows, and recomputes the session counts and the catering from those same rows. It leaves the form responses alone (the tombstone, not a deletion, is what keeps them from coming back as rows), and the mark itself is protected from re-derivation in `getProtectedRegistrantKeys()`, without which an hourly sync between the mark and the sweep quietly resets it to `Auto-Synced`.<br><br>The sweep also appends a `removed` entry per registrant row (`appendRemovalLedgerEntries_`, `99k`) — `removed` and not `cancelled`, because a cancellation is a fact about a person and a removal is a fact about a mistake, which is why these rows do not give their seat back the way a cancellation does. Only for `All_Registrants`: the other tab it clears is `Deleted_Event_Triage`, whose rows are the record of a registration that already ended. |

### Where the generated files live (82)

Behavior plus its own two constants, numbered last for the usual reason:
nothing else derives from them, and `04`, `05`, `45`, `46` and `55` all reach
it through hoisted function declarations. It was `79` on its own branch until
the merge that brought `79`–`81` in; the prefix is all that changed.

| File | | What is in it |
|---|--:|---|
| `82_drive_organization.gs` | 427 | **The anchor.** Every folder this system keeps was found with a Drive-WIDE `getFoldersByName` and created with a parent-less `createFolder` — that is, in My Drive root. `getSystemRootFolder()` is the folder the WORKBOOK sits in, remembered in Script Properties, and `getOrCreateSystemFolder()` is the one lookup all five now share: inside the anchor first, adopting (and renaming) a stray folder of that name if there is one — never duplicating it, because the files in it have live links — and creating inside the anchor otherwise. `moveDriveFileInto()` is `50`'s `moveTo`-then-`addFile` pair generalized, and never throws. Plus `organizeGeneratedFiles()`, the Admin one-time sweep: everything with an id in a registry, then My Drive root by name AND MIME type (`STRAY_FILE_PATTERNS`). |

### One row per month (83)

Numbered after `82` (the Drive-organization file) for the usual reason — never
renumber, and this landed last. Safe there: behavior only, its schema
(`SHEET_NAMES.METRICS`, `HEADERS.Metrics`, `METRICS_STAFF_COLUMNS`) lives in
`03` like every other tab's, its own `const` stands alone, and everything it
calls into `34`, `40` and `43` is a hoisted function.

| File | | What is in it |
|---|--:|---|
| `83_monthly_metrics.gs` | 714 | The `Metrics` tab: one stored row per calendar month, and the year-over-year block built on those rows rather than on `All_Registrants`. **The rule the file exists for:** a month whose registrant rows have been archived recounts to `null`, not to zero — overwriting a captured month with a count of nothing would report a collapse that never happened. It is the ONE tab in this workbook that is a record rather than a projection of the rows still on the other tabs, and `collapseOldPastMonths` is why it has to be. Counted through the same readers `43` uses, so the two blocks cannot disagree about what a registration is. Since `99e` it also stores `Volunteers` / `Volunteer_Visits` / `Volunteer_Hours`, counted off `Volunteer_Hours` — people once and visits every time, because the annual return asks both — and a month with volunteers and no registrations is a month this tab now captures. Runs monthly (`captureMonthlyMetricsTrigger`, the 2nd at 4am) and on demand (`refreshMetricsTabNow`, under Settings & Fixes).<br><br>Its own `HEADERS.Metrics` key is spelled for the TAB, unlike the session table's — there is no second tab called `Metrics` for it to collide with. |

### Session rows left behind by a calendar (84)

Behavior only, numbered last for the usual reason: it declares no constant
anything else derives from, and everything it reaches for — `CALENDAR_MAP`,
`HEADERS`, `renderProgramDashboard`, `moveRegistrantsToTriage` — it reads at
CALL time or through a hoisted function declaration. It was `83` on its own
branch until the merge that brought the `Metrics` tab in; the prefix is all
that changed.

| File | | What is in it |
|---|--:|---|
| `84_orphaned_session_rows.gs` | 297 | **A calendar that left, not a date that did.** `triageDeletedSessions` (`43`) removes a session whose calendar EVENT went; by design it leaves alone any row it cannot attribute to a calendar it just read, which is what stops one unreadable calendar cancelling a location — and which also means a retired, recreated or repointed calendar leaves a residue nothing reaches, re-importing beside itself under the new ID. `findOrphanedSessionRows` is the pure split, scoped by `Calendar_Source` alone: a blank one is a lunch or hand-typed row and is never in scope, and a CONFIGURED calendar's rows never are either — nothing here reads a calendar, so a calendar that merely failed to load cannot be read as a retired one, which is also why this needs no size limit where triage does (a retired location legitimately IS most of the table). `describeOrphanedSessionRows` names each calendar ID in full with its rows, programs and date span, and is shared by `reportOrphanedSessionRows` (read-only, ungated) and the confirmation `removeOrphanedSessionRows` asks — menu-only, never a trigger, gated as `Remove Leftover Calendar Rows`. Registrants MOVE to `Deleted_Event_Triage` rather than being deleted, which is why `moveRegistrantsToTriage` (`26`) now takes optional wording: "the event is gone" would be a lie about a session whose event may still sit on a calendar we no longer read. The retired calendar's forms are left alone — a link already in circulation still opens. |

### Duplicate registrations (85)

Numbered after `84` for the usual reason — never renumber, and this landed
last. Safe there: its own constants stand alone,
its schema is `HEADERS.All_Registrants` in `03` like every other tab's, and
everything it calls — the tombstones (`28`), the name corrections (`77`), the
render and the two recounts — is a hoisted function.

| File | | What is in it |
|---|--:|---|
| `85_duplicate_registrations.gs` | 625 | **One person, one session, two rows** — the form plus a walk-in, a form submitted twice, two spellings of one name. Two rows are two seats against a capacity and two meals against a catering count, so `showDuplicateRegistrationsDialog` lists what looks like a duplicate and collapses only what somebody ticks: a REVIEW, not a sweep, because two rows for one name are usually a duplicate and sometimes two people, and nothing here can tell those apart. The key is deliberately GENEROUS, because a group somebody unticks costs a glance and a duplicate never offered costs a seat: the SESSION (`duplicateRegistrationSessionKey` — date + building + canonical title, falling back to Event_ID, so a form row and a door row on one class still pair up when an appointment day is typed as one event per slot or a `[Shared]` program one per building) + `Person_Type` with a blank read as Registrant (what the door writes) + the name worn down to first and last (`duplicateRegistrationNameKey` — the parenthetical off via `parseMemberName`, an earlier correction applied via `canonicalMemberName`, then accents, punctuation, suffixes, "Smith, Bob" and the middle name one form asks for and another does not). A `Cancelled` or `Superseded` row never groups with a live one, because "they cancelled and signed up again" is what `71` exists to keep. The merge is additive in every column (`mergeRegistrantRow`): marks OR-ed, notes joined, the most active status kept, and meal counts MAXIMUM or SUMMED as the dialog is told (`DUPLICATE_REGISTRATIONS_MEAL_MODES` — one person twice is one meal, two people under one spelling is two, and nothing here can tell those apart, so it asks; `Party_Size` adds with them). What makes it stick is the pair at the end — a dropped row whose IMPORT key (`registrantImportKey` — Event_ID | spelling | Person_Type, which is stricter than the grouping key and is what `28` matches on) differs from the survivor's is tombstoned, since the next sync would otherwise write it straight back — that now includes a row on a second Event_ID, not only a second spelling — while a spelling difference also remembers the correction (`77`). Two rows sharing one import key need neither, and a tombstone there would suppress the row that was kept.<br><br>The collapse now appends a `merged` entry per pair carrying the merge's OWN arithmetic — the meal mode applied, the marks OR-ed, the status kept — because by the time anybody folds the ledger the two rows cannot be seen separately and a replay that re-derived would disagree with what the desk was shown. The survivor's id is resolved BEFORE anything is folded into it: `mergeRegistrantRow()` fills the survivor's blanks from the row it absorbs, including that row's `Event_ID` on a group spanning two. |

### The office's one email a day (88)

Behavior only, and numbered last for the usual reason: its own constants stand
alone, it reads no other file's constants at load time, and its callers (`15`,
`33`, `66`, `70`) reach it through hoisted function declarations.

| File | | What is in it |
|---|--:|---|
| `88_office_daily_digest.gs` | 427 | **Everything the office is told, spooled, and sent once at ten.** The office was on four mailing paths at once — the per-sync digest (`15`), the per-category notices (`55`), a forwarded copy of every registrant reminder (`70`) and a BCC on every leader alert (`66`) — which on a busy day was forty messages, and forty messages is the same as none. `spoolOfficeNote()` is the one way anything reaches the office now: a section, a line, and the time, written to Script Properties as it happens (one property per chunk per day, because a value there is capped at 9KB; one WRITE per note and no read, because the day is held in memory). `sendOfficeDailyDigest()` is the 10am trigger — it sends every spooled day STRICTLY BEFORE today and deletes it, which is what makes a missed trigger cost nothing (Friday sends Tuesday and Wednesday, in two messages, each headed with its own day) and what stops a note written at 09:59 being split across two digests. The spool is cleared only once the message is away. Sent to EVERY address on Config's Admin Notification Emails table, ticked or not, and never rationed or paused — like `notifyAdmin` always was, and for the same reason.<br><br>**The same note twice is one line and a count** (`spoolOfficeNote` coalesces on section + message, keeping the first and last times), which is the difference between a digest of twelve lines and one that says the same twelve things two hundred times. **What does NOT wait** is `notifyAdminUrgent` (`15`) and its two callers only — a Quick Mark that would not save (`38`) and a door sign-in that did not complete (`72`) — because somebody is at the desk. Adding a third is a decision, not a detail. The per-category ticks on Config therefore govern only who gets those; four of the five route nothing now and their cell notes say so rather than pretending otherwise. |

### The public program calendar (86–87)

The one page in this project opened by somebody who is not staff, not at the
door, and not already registered — so the one that can be printed on a flyer.
Numbered last for the usual reason: both are behavior only, their own
constants stand alone, and everything they reach for (`HEADERS`,
`getSectionedRows`, `deskMonthHorizonKey`, the tag readers, `tryGetScriptCache`)
they read at CALL time or through a hoisted function declaration. `60`'s
`DOOR_ROUTES` gains a `public` entry that names them, which is lazy for
exactly this reason.

| File | | What is in it |
|---|--:|---|
| `86_public_program_calendar.gs` | 539 | **What is on, and the form to sign up with — for a stranger.** `publicProgramCalendar()` is the ungated read behind `?mode=public`: every session from today to the end of NEXT month (`deskMonthHorizonKey`, the same horizon the door's month picker uses), each carrying the CURRENT registration link for that session — which for a Regular program is the thing an emailed link is always wrong about a month later. It is safe to publish because of what `buildPublicSessionRow()` does NOT put in it: **no names, no counts of who, no `All_Registrants` read at all** — only what is already printed on a calendar event a member can see, plus whether there is room (`publicSeatsPhrase_`, exact at the bottom end and vague at the top: "3 seats left" changes an afternoon, "37 registered" is an internal number). Rows that must not be shown are dropped SERVER-side (past, beyond the horizon, untitled, `Cancelled`) — a filter in a public page's JavaScript is not a filter — and `publicSessionState_` resolves the four things a stranger can be told: sign up, join the waiting list, just come along (`No_Registration`), or not yet (the sessions are on the calendar but no form is generated, which is ordinary a month out). The link is read out of the `=HYPERLINK()` FORMULA in `Form_Response_Link`, because the cell's value is the words "View Live Form" — and for the same reason the TIME cannot be read out of its cell either: `Event_Time` is a formula (`setEventTimeFormulas`), so `publicSessionTimeLabel_` rebuilds the label from the row's own start and end rather than publishing `=IF(W9="",TEXT(…))` where the time should be. Each row also carries the `programKey` the page groups on (title + building) and the `shortLabel` its date chips are drawn with; a lunch-only row is renamed `Lunch` (`PUBLIC_LUNCH_PROGRAM_TITLE`) so lunch is one program per location instead of a tile a day called "Lunch Only (no program)". The snapshot also carries an `intro` — `CENTER_NAME` / `CENTER_PHONE` / `CENTER_EMAIL` from `04` (the same three the forms and the sign-in sheet print, so a changed number cannot be right on a form and wrong here), the one editable sentence `PUBLIC_CALENDAR_INTRO`, and the addresses of the buildings that actually have something on: this is the one page whose reader may not know whose calendar it is. Cached in CacheService for `PUBLIC_CALENDAR_CACHE_SECONDS` under a key with today's date in it — a TTL alone would go on offering yesterday as the first day past midnight. **No PIN, deliberately**: it writes nothing, and a page that asks a stranger for a staff code is a page that becomes a phone call.<br><br>**And the embed (17b), which is the same route and not a second page.** `?embed=1` takes the chrome off — no introduction, no background of its own, no sticky bar — so the calendar inherits the website around it, where a Google Calendar embed would otherwise sit in Google's own furniture and be unable to say whether there is a seat or open THIS month's form. `publicCalendarViewOptions` is the only thing read off the query string, and it changes how the page is DRAWN and nothing about what it contains: `?building=` pins the building and `?span=` the range — the same `?span=` the two printed links carry, read by the same `publicCalendarSpanRequested_`, so a spelling that works in a newsletter works in an iframe. A building the snapshot has never heard of is dropped rather than obeyed (an empty calendar on a website is the failure nobody reports, because it looks deliberate). `publicCalendarEmbedUrl` / `publicCalendarEmbedSnippet` build the `<iframe>` and the eleven-line resize listener staff paste into their site — the listener answers only its own frame and only `PUBLIC_CALENDAR_EMBED_MESSAGE`, and the iframe's own `height` is the fallback for a host that never hears it. |
| `87_public_program_calendar_html.gs` | 688 | `buildPublicCalendarHtml` — the served page, one template literal. **One TILE per PROGRAM, two to a row, not one card per date**: the tile names the program, says when it runs in one line (`scheduleLine` — "Tuesdays & Thursdays · 9:30 AM – 10:30 AM", or the count when the rhythm is not one), and carries its dates as chips, each its own link to that session's form (`+N more` for a long run). **The whole rectangle is the button** — a `role="button"` div rather than an `<a>`, because a browser will not honour an anchor nested in an anchor, with `tabIndex`/Enter/Space wired by hand and the chips' clicks stopped from reaching it. It opens the next date somebody can ACTUALLY get into (`leadOf`), which is the same rule the words follow: **"Join the waiting list" is said about a program only when every date in view is full** (`everyDateFull`) — a class with four open weeks and one full one is a class you can come to, and a tile labelled off its next date sends that person away from something that was theirs. The full dates are coloured amber instead (`.chip.full`), with a one-line legend on the tiles carrying both kinds. **Lunch is pinned** ahead of everything and drawn full width: it runs nearly every weekday at both buildings and in date order would sit wherever tomorrow happens to fall. Above the grid, the page introduces the centre from the snapshot's `intro` — name, sentence, a `tel:` link a thumb can dial, and the buildings with their addresses. **The whole two-month window is inlined**, so the first frame is the answer and the week / month / everything filters, the building filter and the search are arithmetic in the browser rather than a round trip apiece; the only server call is the quiet re-read after the first paint (and the Refresh control), which never redraws under a finger. `?span=week` / `?span=month` (resolved in `60` by `publicCalendarSpanRequested_`, written by `checkInPageUrl({ mode: 'public', span })`) only choose which filter is already pressed on open — which is what makes a weekly newsletter link and a monthly flyer link two addresses of one page rather than two pages. A tap marks the tile (or the chip) as opening in the same frame and the form opens in its own tab, so the calendar a person spent four taps filtering is still behind it. Everything from the workbook is written with `textContent` — the page uses no `innerHTML` with data in it at all — and the snapshot crosses into script through the same double-`JSON.stringify` every other page here uses. **Embedded (`?embed=1`), it also reports its own height** (`postMessage`, after every draw and through a `ResizeObserver`), which is what stops the scrollbar INSIDE the frame — on a phone, the gesture where somebody scrolls the calendar meaning to scroll the website and concludes the site is broken; a tile expanded to show the rest of its dates is exactly the case that needs it. The embed skin is written SERVER-side rather than switched on by a class the page's own script adds: a class added after the first paint is an introduction drawn, seen and then removed inside somebody else's website. A page with a pinned building also stores nothing, since the embed and the printed link share one origin and one storage key. `tests/public_calendar.test.js` pins the field list that may leave the workbook, the four states, the grouping, the time, the introduction, the two-column grid, the lunch pin, the whole-tile button and the waiting-list rule, the routing, which routes may be framed and what the embed pins; `tests/inline_pages_parse.test.js` compiles its script block, framed and unframed. |

### A calendar sync that runs out of time finishes later (90)

Numbered 90 because 89 went to the sharing sweep in the merge that brought both
in. Safe there: behavior only, its own
five constants stand alone, it reads no other file's constants at load time,
and everything it calls (`syncCalendarsInternal`, `runSlicedJob`,
`getSyncSliceBudgetMs`, `automationGateAllows`, `isBootstrapActive`) it reaches
through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `90_calendar_sync_slices.gs` | 285 | **`syncCalendars()` as a sliced job.** The group loop is the expensive part — a Drive copy, a dozen-plus Forms calls and a description write per event, per group — and on a workbook with a hundred programs it is longer than one execution. Apps Script kills the run at the account's ceiling with no warning, no exception and no `finally`, and what the kill cost was never the groups already done (their rows, forms and links are on the sheet) but everything AFTER the loop, every single time: the registration horizon never applied, the link cells never written, the dashboard never redrawn, the lunch forms never built — and `withCalendarChangeTriggersPaused()`'s `finally` never reached, which leaves the calendar-edit watchers DOWN until the next sync that manages to finish. The next run then started from the top and was killed in the same place.<br><br>So a slice works to Config's `Sync_Minutes_Per_Run` (the same dial `98` reads), stops BETWEEN groups (never mid-group: a group's rows, its form and its calendar description have to land together), and hands the rest to `resumeCalendarSync` a minute later. The state machine — watchdog armed before any work, deadline, slice counter, stall detection, hand-off — is `runSlicedJob` (`75`).<br><br>**There is no plan to keep, and that is the design.** Unlike `98`, this job stores no list of what is left: `collectCalendarWork()` derives the work from the dates NOT already on the session table, so a group this slice finished is one the next slice skips. The sheet is the record — the same bargain `25` has always made, and why progress is judged against the REMAINING count rather than by groups touched. The cost is one calendar re-read per slice; the benefit is that a slice killed outright leaves no stored plan disagreeing with the workbook.<br><br>**What a partial slice deliberately does not do**: triage (`renderProgramDashboard(false, { skipTriage: true })` — a pass that moves registrants to `Deleted_Event_Triage` must not judge a calendar this run did not finish working through, and it is a second calendar read a spent budget cannot pay for), the lunch sign-up forms (they build forms of their own), or claim to be complete. The slice that finishes the window does all three. `syncCalendarsInternal(options)` now takes a `deadline` and returns the import summary; with no deadline it runs the whole window exactly as it always did, which is what `58`'s own update calls. `tests/calendar_sync_slices.test.js` pins the deadline reaching the loop, the unsliced path, both halves of the partial-run rule, the hand-off and its counters, the stall stop, the spent trigger, the kill switch and the skipped slice. |

### The public embeds and the weekly page (91–93)

The two pages that live on somebody ELSE'S website, and the property that
separates them from everything else this deployment serves: they may be
FRAMED. Numbered after `88`–`90` for the usual reason — never renumber, and
these landed last — and safely so: all three are behavior only,
`PUBLIC_EMBED_ROUTES` is lazy (`01a`), the mode, span and weekday constants
stand alone, and every page they name is a hoisted function.

| File | | What is in it |
|---|--:|---|
| `91_public_embeds.gs` | 442 | **The public router, the shared look, and the one place `ALLOWALL` is written.** `PUBLIC_EMBED_ROUTES` declares both public pages — `PUBLIC_CALENDAR_MODES` and `PUBLIC_CALENDAR_SPANS` / `publicCalendarSpanRequested_` moved here from `60`, plus `PUBLIC_REGULAR_MODES`, which deliberately does NOT claim 'weekly': `?span=weekly` already means the calendar's next seven days, and one word meaning two things across two links printed side by side is a link pasted wrong. `publicEmbedRoute` claims a request, `publicEmbedUrlMode` is what keeps a printed `?mode=` and this table together, and `servePublicEmbed` is where `XFrameOptionsMode.ALLOWALL` lives — it was a `frameable` flag on a row of `DOOR_ROUTES`, which is a property of the RESPONSE described on a page; every route left in that table writes to the workbook, so the permission now belongs to the file that only serves pages which cannot. Also `publicEmbedStyles()` / `publicEmbedSkinStyles()`, the stylesheet BOTH public pages are drawn on: transparent body (the website's own block colour is the page), white self-painting surfaces, black pill buttons, an outlined building chip, and **no `prefers-color-scheme` block** — the old one turned the text pale on a phone set to dark while the green block behind it stayed green, which is why the "3 programs · 12 dates · Refresh" line could not be seen. Plus `PUBLIC_LOCATION_COLORS` — the website's own two colours doing a job, green for Ashbridge and yellow for Narberth, keyed on the lower-cased building name and inlined into both pages by `publicLocationColorScript()`: the building's chip is FILLED in it and the tile carries a stripe of it down its edge, because on a screen of tiles "which one is at Narberth" is scanned rather than read. A building this map has never heard of keeps the neutral outline rather than borrowing a colour that means something else on the site. The pages take the width they are given (three tiles a row past 1100px, seven weekday columns on `93`), because an embed is read landscape on a laptop as often as portrait on a phone. And there is **no "Sign up" pill on a tile**: the whole tile is already the button, so that slot now carries only the states a tap cannot answer — no sign-up taken, no form yet, waiting list. Plus `publicRegularEmbedUrl` / `publicRegularEmbedSnippet`, the second page's own snippet with its own frame id, so a site embedding both grows two frames. |
| `92_regular_programs.gs` | 193 | **What runs every Tuesday, decided from the dates.** Nothing in this workbook stores "this is a weekly class" — the recurrence lives in the calendar — so `foldRegularPrograms_` reads it off the sessions: same weekday, gaps a whole number of weeks, smallest gap exactly one (the rule `detectProgramMonthRecurrence` in `78` uses, and for the same reason — a weekly class that misses a week has gaps [1, 2, 1]), at least `PUBLIC_REGULAR_MIN_SESSIONS` of them. Every OTHER week is deliberately not here, and neither is lunch (it runs nearly every weekday and is pinned on the calendar page already). Grouped on the server's own `programKey`, so a `[Shared]` Tuesday program stays two programs at two addresses and the two public pages cannot disagree about what one program is. Built by folding what `publicProgramCalendar()` already returned — one tab read, one cache, two pages — and its programs carry the calendar's own session rows untouched, so nothing new leaves the workbook. |
| `93_regular_programs_html.gs` | 464 | `buildPublicRegularHtml` — the served page, on `publicEmbedStyles()` and the same tile `87` draws (whole-tile button, date chips each carrying their own form, the waiting-list rule read across every date in view). **The week is COLUMNS** — one `.daycol` per weekday that has anything on (`repeat(auto-fit, minmax(190px, 1fr))`, so a centre that runs nothing on Sundays prints no Sunday), each headed **Every Monday** … **Every Sunday** with its programs stacked under it; folding to one column on a phone. The weekday is the column, so a tile's own line drops it and says the time and how many dates are left instead, and its date chips cut off at four rather than six because a tile here is a fifth of the screen wide. Takes the same `publicCalendarViewOptions` the calendar does — `?embed=1` and `?building=` mean the same thing on both pages — and reports its own height the same way, so a host frame grows instead of scrolling. `tests/regular_programs.test.js` pins what counts as regular, the field list, the routing, the embed pins and the framing; `tests/inline_pages_parse.test.js` compiles its script block. |

### A recurrence that ends is a series (95)

Numbered after `94` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own five constants stand alone, its one
Config cell lives in `04` like every other setting, and everything it reaches
for (`parseSettingsBrackets`, `setGroupingBracketInDescription`,
`writeFormIdOntoSessions`, `getSectionedRows`) it reads at CALL time or through
a hoisted function declaration. Four earlier files call into it the same way:
`20` (the third grouping layer), `21` (the stamp and the work-list reason),
`24` (the form a newly grouped program keeps, and the adoption) and `15` (the
Config cell's seed and reader).

| File | | What is in it |
|---|--:|---|
| `95_recurring_series_detection.gs` | 714 | **[Grouped] is the one tag nobody has to type.** An eight-week course goes on a calendar as one event repeating weekly, ENDING AFTER 8 OCCURRENCES — a complete statement about what the program is, which this system read as eight ordinary dates and, when the run crossed a month, split onto TWO forms with the roster down the middle. `isDetectedGroupedSeries()` reads the recurrence instead: bounded (a `COUNT` or an `UNTIL`) and at most `getGroupSeriesUpTo()` occurrences — twelve by default, `0` to turn the whole thing off, on Config's `Recognize Short Series`. **The ceiling is the point**: a standing Tuesday class typed as "every week until December 31st" is 52 occurrences of an ordinary program, and one form for all of it is a year of dates on one page beside a capacity that means nothing. `resolveEventSettings()` (`20`) reads this LAST, under the description's own bracket and under a legacy title bracket, so a typed `[Grouped]`/`[Regular]` — and the dashboard's Type_Tag dropdown, which writes exactly that — always wins.<br><br>**Counting the rule.** `CalendarApp` cannot answer this at all (`isRecurringEvent()` is the whole of what it exposes), so `readSeriesRecurrence()` asks the advanced Calendar service the project already uses for its sync tokens (`19`) — one `Calendar.Events.get()` per SERIES per execution, memoized here rather than in `08` on `74`'s precedent, and nothing at all for the single events that are most of a calendar. An event that came from an .ics import keeps the ORIGINATING system's UID, which is not the API's event id, so that `get` 404s: `readSeriesMaster()` answers the 404 by listing the calendar by `iCalUID` with `singleEvents` off, which hands back the master in one further call — only for the imported events that need it. `countRecurrenceOccurrences()` takes a `COUNT` as written and WALKS an `UNTIL`, stopping at `RECURRING_SERIES_COUNT_LIMIT`; it is approximate in one direction only, and deliberately: `EXDATE` is ignored (a cancelled occurrence only makes a run shorter, so ignoring it can only refuse to group), a monthly rule is stepped by months rather than resolved through `BYDAY`, and two `RRULE`s or any `RDATE` returns null — a shape it cannot claim to understand keeps the monthly forms staff already have. The `Z` on an `UNTIL` is honoured as UTC, because reading it locally puts the bound four hours late and that is one occurrence at the boundary.<br><br>**And it is written down.** `stampDetectedSeriesGrouping()` puts `[Grouped]` into the events' descriptions ONCE, from the sync (`21`), for three things a derived answer cannot do: be SEEN (a program grouped for a reason nothing on the calendar states is one nobody can explain), be ARGUED WITH where staff argue with everything else, and SURVIVE a recurrence extended in January to run all year — which would otherwise re-split a course mid-run and strand its roster. `AUTO_GROUPED_SERIES_PROP_KEY` is what makes "once" true: deleting the bracket is an answer too, and a stamp that came back every hour would be this system arguing with somebody about their own calendar. A description that already states grouping is never touched in either direction.<br><br>**Adoption, because every program this reaches already has forms.** A course recognized in week four presents to `processCalendarGroup()` as a group whose `::FIXED` key the registry has never heard of — and a group with no form gets a NEW one, which is a new link on a course three weeks in. So `chooseFormForGroupedProgram()` picks the form its NEXT date is already on (the link in circulation, the same rule `chooseAdoptedAssistanceForms()` follows) out of `programFormCandidates`, the per-PROGRAM form map `getExistingRegistryState()` now also builds; `adoptGroupedProgramSessions()` moves the upcoming rows onto it, past rows left alone as the record of where each registration came from, superseded forms neither deleted nor hidden. It also writes `Type_Tag` on those rows (`writeTypeTagOntoSessions`), without which `formSpanForRow()` keys them back to their own month and the next pass restores last month's link. The MOVE is gated on `groupedProgramNeedsAdoption()` and the tag is not: both halves — rows on more than one form AND an upcoming row still saying Regular — are required, so a date somebody deliberately moved off a settled series (`47`) is left where they put it instead of being dragged back every hour. `tests/recurring_series.test.js` pins the rule reading, the ceiling, the precedence, the stamp and its ledger, and both halves of that gate. |

### One form for an appointment program (94)

Numbered after `93` (the weekly programs page) for the usual reason — never
renumber, and the prefix is all that changed when the merge that brought
`88`–`93` in took the number this file was written under. Safe there: behavior
only, its own two constants stand alone, and everything it reaches for (`HEADERS`,
`getSectionedRows`, `writeFormIdOntoSessions`, `coerceDate`, `getMonthLabel`) it
reads at CALL time or through a hoisted function declaration. Five earlier files
call into it the same way: `07` (the span), `09` (the form context), `20` (the
calendar read window), `24` (the fold, the trim and the adoption) and `25`/`26`
(what the link and the form title say when there is no month to name).

| File | | What is in it |
|---|--:|---|
| `94_assistance_program_forms.gs` | 341 | **A [Personalized Assistance] program is one form, three months wide.** It was one form per calendar MONTH like everything else — twelve forms a year for one standing arrangement, and a link handed out in September opening a form in October with no free slot on it, because it was never about October. The span half of its group key is now `ASSISTANCE_FORM_SPAN` (`'ASSIST'`, a persisted internal key like `'FIXED'` beside it and renamed for the same reasons: never), so every month of one program folds onto one form — `mergeAssistanceProgramGroups()`, which MUST run after `unifyProgramFlagsAcrossGroups()` because the flag it groups on is only true of every month of a program once that has run.<br><br>**The window is what keeps it finite.** A [Grouped] series ends; an appointment program does not, so an unbounded form would grow a time question hundreds of choices long, most of them in the past. `assistanceFormWindow()` is this month through the end of the `ASSISTANCE_FORM_MONTHS`-th (3), stated in MONTHS because a form whose dates move under the desk mid-month disagrees with every sheet printed that morning. It is enforced in two places that have to agree — `trimGroupsToFormWindows()` on the CALENDAR-derived sessions (24) and `windowAssistanceContextSessions()` on the ROW-derived ones (09) — because `refreshFormShapeForAllForms` (`31`) rebuilds a form's labels from its rows every sync while `refreshFormForNewDates` (`26`) rebuilds them from its group: two windows would be each pass undoing the other's write, hourly, forever. `trimGroupsToFormWindows()` takes its two bounds as options, defaulted to those two windows, so the weekend loader (`80`) — which reads a window somebody TYPED into two date boxes — can say that its own end is the end, and a date a person ticked is never dropped because an unattended hourly pass would not have reached it. The context trim reaches `sessions` ONLY; the fields that decide how a date is LABELLED stay derived from the whole row set, because `buildRegistryIndex()` (`27`) matches a response back by exactly those labels.<br><br>**Reading further is not promising more.** `computeSyncDateRange()` (`20`) now returns `ordinaryEnd` beside `end`: whether an event is an appointment event cannot be known until it has been read, so the read reaches as far as the window needs and every ordinary program in that extra reach is trimmed back to the horizon it has always had.<br><br>**And the migration ships with it**: `adoptAssistanceProgramSessions()` moves a program's UPCOMING rows onto the one form — chosen by `chooseAdoptedAssistanceForms()` (`24`) as the form the program's NEXT session is on, so the link in circulation is the one that survives — through `writeFormIdOntoSessions()` (`47`), the same writer the "Move Sessions" dialog uses. Past rows are left alone, because a past row's `Form_ID` is a record of where that registration came from. `getExistingRegistryState()` marks such a program in `splitAssistancePrograms`, which is the only reason `collectCalendarWork()` (`21`) processes a program with no new dates — otherwise adoption would wait for the next month to appear on the calendar. |

### An optimistic write that did not land (99b)

Behavior only, numbered last for the usual reason — never renumber, and this
landed last. It was `97` until the merge that brought the render-batching file
in took that number. Safe there: its own constants stand
alone, its schema is nothing (it stores its own queue), and everything it calls
— `readCheckInList` / `writeCheckInList` / `withCheckInQueueLock` (`63`),
`applyQuickMarkLocked` (`38`), `walkInSignIn` (`74`), `notifyAdminUrgent`
(`15`) — is a hoisted function.

| File | | What is in it |
|---|--:|---|
| `99b_optimistic_retry.gs` | 280 | **The three places that answer before they write, and what happens when the write then fails.** Quick Mark's dialog (`36`), the household press (`38`) and the door app (`72`/`73`) all hand themselves back before the sheet is touched, so a refusal has nobody looking at it; all three used to mail the office at once (`notifyAdminUrgent`, `15`). But the commonest refusal by a wide margin is the workbook being mid-sync — `withScriptLock` gives up after `DESK_LOCK_WAIT_MS`, the hourly import holds it for longer, and the answer is a mark that would have worked two minutes later. So `queueOptimisticRetry()` writes the caller's arguments VERBATIM (Quick Mark's args run to meal counts, standing places and appointment times, which is why this is not `63`'s five-field queue — though the chunked storage, the lock and the caps underneath are `63`'s) with a `kind` saying which writer they belong to, and arms a one-off trigger `OPTIMISTIC_RETRY_DELAY_MS` out; `flushOptimisticRetryQueue()` applies what it can and re-arms for the rest, so a mark refused during an hour-long import lands by itself once the import is over. **The bound is not a hedge**: "retry until it works" is right for a busy workbook and wrong for a mark that can never work (a name nobody registered, a lunch on a date with no lunch), so after `OPTIMISTIC_RETRY_MAX_TRIES` the original urgent mail goes out with the whole history attached — the retries were added, nothing was taken away. **A busy lock is not a try**, or a long sync would burn the budget on collisions with nobody. And the batch is SPLIT by whether the writer takes the script lock itself: `LockService` locks are not reentrant, so calling `walkInSignIn` (which takes it, via `applyQuickMarkFromDialog`) from inside a lock this flush holds would not deadlock — it would do something quieter and worse, returning "the workbook is mid-update" from a collision with ourselves. `flushCheckInQueueTrigger` (`63`) flushes this queue too, as the safety net for a one-off trigger that could not be created. `tests/optimistic_retry.test.js` pins the queue-not-mail rule, the silent success, the free busy try, the cap and its mail, and the unlocked door path. |

### One read of a tab, one write (96)

Numbered after `95` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its one constant (`SESSION_GRID_HEADER_MARKER`)
stands alone, its schema is `HEADERS` in `03` like every other tab's, and
everything it calls — `coerceDate`, `normalizeHeaderText`,
`invalidateSectionedRowsCache` — is a hoisted function. Five earlier files
reach it the same way: `07` and `34` (the four generic readers), `08` (the one
invalidation), `21` (the scope the sync opens) and `22`/`23`/`30` (the passes
inside it).

| File | | What is in it |
|---|--:|---|
| `96_session_grid.gs` | 386 | **The reconcile phase used to read the session table six times to answer six questions about it.** Each pass (`22`'s two flag reconciles, `23`'s times, assistance settings, `[No Registration]` effects and link columns, `30`'s counts) began with `findProgramSessionHeaderRows()` — which reads the WHOLE grid to find the two rows holding the word `Event_ID` — then read three to nine single columns PER SECTION ZONE and wrote back a column at a time. Every one was correct and cheap alone; six together, on a workbook of 432 session rows, measured at 142 round trips and 86,602 cells (`tools/sync_bench.js`). It is 16 now, and the five passes the sync runs together share ONE read.<br><br>**Two layers.** `readSheetGrid()` is the raw one: at most two calls per tab per execution — the values, and the formulas only if somebody asks — with the values kept so a formula reader that comes second pays for the formulas alone. `readSectionedGrid_` (`34`), `findAllHeaderRows` / `getHeaderMapAt` (`07`) and `getZoneDataRange` (`34`) all project from it now rather than fetching their own, which is why a render, a triage pass and an import reading the same tab in one execution cost one fetch between them instead of one each. `loadSessionGrid()` is the layer above: the same read carrying ROW POSITIONS, which is the thing `getSectionedRows()` deliberately does not give (it answers "what rows are on this tab?", projected into `HEADERS` order, which is right for a render about to write every row back and wrong for a pass changing four cells in a column of four hundred) — and the reason those passes read raw columns in the first place.<br><br>**`withSessionGrid()` is the scope, and the flush is the point.** `21` opens one around its five passes; each stages into shared mutable column arrays and the scope writes back once, merging adjacent dirty columns into a single call. **The order of effects is unchanged**, which is the property this had to keep: a pass reading a column an earlier one has just written finds the new value because they hold the same array, where before it found it because the sheet had been re-read. Only the moment the SHEET is told has moved. Outside a scope each pass loads and flushes for itself — `applySessionTimesToRows()` and `applyAssistanceSettingsToRows()` are also called from `56` and `58`, where the write has to land before the function returns.<br><br>**Formulas are read alongside the values, and that is what unlocked the worst of it.** `updateRegistrationLinkCells()` wrote two or three cells PER ROW with `setValue()` because `Form_Response_Link` holds `=HYPERLINK()` and `getValues()` flattens a live link to the words "View Live Form" — so a whole-column write would have turned every untouched link on the column into dead text. Reading the formulas (the same merge `readSectionedGrid_` has always done) removes the reason for that, and the column goes back in one call with every link still a link. `tests/reconcile_batching.test.js` pins exactly that, along with both zones, the date-keyed session flag and the appointment arithmetic; none of those six functions had a test that executed them before.<br><br>**Invalidation is the whole risk**, and it is answered by not having a second list: `invalidateSectionedRowsCache()` (`08`) drops the grid too, so the twenty-six places that already invalidate on a write need no change. The cache is keyed by tab NAME — what those call sites have to hand — but checked against the sheet OBJECT, because a name is not an identity and serving one sheet's grid to another is not a stale read, it is somebody else's tab. |

### One tab rewrite, one call per attribute (97)

Numbered after `96` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its two module-level `let`s are plain nulls
and an empty object, and everything it reads from other files (`PALETTE`, and
`log`) it reads at CALL time or through a hoisted function declaration. Five
earlier files reach it the same way: `07` (the tint, the three validation
helpers, the manual-entry label and wash), `13` (the zebra striper, the section
header, `autosizeColumns`), `34` (the two bands and the scope
`renderFlatDateSheet` opens), `39` (the registrant tab's checkbox and meal
columns) and `43` (the session table's).

| File | | What is in it |
|---|--:|---|
| `97_render_batching.gs` | 414 | **A render's cost was never its rows.** On the Registrants tab at a year of registrations — 1,280 rows, 37 columns — the `setValues()` that puts every row on the tab is FOUR round trips out of nearly five hundred (`tools/render_bench.js`). The rest was formatting applied a column at a time: a background matrix, then a month tint that RE-READ the date column it had just been handed, then one `setBackground` per manual-entry column, then a `setDataValidation` per dropdown and per checkbox column with an alignment and a number format beside most of them, then four calls per manual-entry column on each of the two header rows — all of it twice, once per zone.<br><br>`withRenderBatch()` is the same bargain `withSessionGrid()` (`96`) makes for the reconcile phase: open a scope, let the passes inside stage into shared arrays, write once on the way out. The arrays are the four attribute PLANES of a band of rows. **Two are written whole and two are not**, and the difference is what an unstaged cell means: backgrounds and validations have a well-defined "nothing here" value (the zebra colour; `null`, which clears) and both tabs clear formats and validations first, so a full rectangle asserts rather than erases; number formats and alignments are written only for the columns that asked, in contiguous runs, because there is no safe "leave this one alone" value in a `setNumberFormats()` matrix. The validation plane shares ONE row array across every row of the band — a validation is uniform down a column and `setDataValidations()` only reads what it is given.<br><br>**Every `stage*` call answers false** when no scope is open or when the range does not sit inside a declared band, and its caller then does exactly what it always did — which is what let the shared helpers in `07` and `13` be converted without touching the twenty call sites that are not a render.<br><br>Also `shouldAutosizeColumns_()`: `autoResizeColumns()` plus one `getColumnWidth()` per column is roughly forty round trips, and a sync renders the same tab more than once, so a tab is sized ONCE per execution unless its last row or column count changed. `tests/render_batching.test.js` pins the three background layers and which wins per cell, the `null` in the validation plane, the header labels, that the zones do not bleed, that a scope which threw writes nothing, and both halves of the protection skip in `39`. |

### A sync that runs out of time finishes later (98)

Numbered after `97` for the usual reason. Safe there: behavior only, its own
eight constants and the tail table stand alone (every `run` in it reaches its
work through a hoisted function declaration, so nothing is read at load time),
its schema is `HEADERS` in `03` like everything else, and `27` reaches it —
and it reaches `27`'s import phase — through hoisted declarations.

| File | | What is in it |
|---|--:|---|
| `98_registration_sync_slices.gs` | 615 | **The longest thing this workbook does, as a sliced job.** `syncRegistrations()` opens every form and asks for its new responses, repairs the forms whose shape has drifted, catches up the "every date" registrants and the club rosters, writes the Registrants tab, recounts capacity, relabels every form and every appointment form's times, rebuilds four dashboards and the memory tabs, creates and pushes a shared roster per program, emails the leaders, invites the registrants to the calendar events and sends the reminders. On a centre with a hundred forms that is more than one execution — and Apps Script does not ask: it kills the run at its ceiling with no warning, no exception and no `finally`. The import survived that (the clock only moves once the rows are on the tab); everything downstream of the kill silently did not happen, hourly, on the workbook that is busiest.<br><br>So the sync works to a budget and hands the rest on. The state machine — the watchdog armed before any work, the deadline, the slice counter, the stall detection, the hand-off trigger — is `runSlicedJob` (`75`), like the four other long jobs here; what lives in this file is what `75` deliberately does not hold: this job's plan, its steps and its words.<br><br>**Two halves, and only one may be interrupted.** The IMPORT (`runRegistrationImportPhase`, in `27`) is the half nothing else will do later: a slice that runs out of budget mid form-loop still writes the rows it built, but does NOT advance the sync clock, and records which forms it never reached — the next slice reads the same window, from the same `lastSync`, for the forms that are left. Only when every one has been read does the clock move to the moment the window OPENED (not to now: a response submitted DURING the sync must be read by the next one). The two form-migration passes wait for that too, because rewriting a form that still holds un-imported responses is how they are lost for good. The TAIL (`REGISTRATION_SYNC_TAIL_STEPS`) may stop anywhere: every step is a rebuild or a send derived from what is on the tabs, so the follow-up runs the ones that are left, IN ORDER — the order is load-bearing in three places the table's own comments name.<br><br>**`syncRegistrationsInternal()` is still here and still unsliced**, with no lock, no budget, no state and no trigger: the four callers that import before destroying something (`32` twice, `49`, `58`) already hold the workbook lock and are already slices of their own jobs, so handing off from inside one would arm a second resume trigger for work the outer job is in the middle of.<br><br>`resumeRegistrationSync` is gated like the sync it continues — pausing automation mid-plan means stop, and the plan goes stale on its own. `tests/registration_sync_slices.test.js` pins the clock rule in both directions, a form that refuses being taken off the list anyway, a write that did not land leaving the clock put, the tail's order and its hand-off, a step that threw being recorded and stepped over, and the unsliced entry point running the whole thing.<br><br>The tail's last step is `ledger_verify` (`99n`), read-only: it folds the registration ledger, reads the Registrants tab and files the differences for the office's 10am digest. Last because it is a statement about the picture this sync SETTLED on, and because a spent budget deferring it costs a day's report and nothing else. |

### A form nobody is importing (99)

Numbered after `98` for the usual reason — never renumber, and this landed
last. It was `97` on its own branch until the merge that brought the render
batching and the sliced sync in; the prefix is all that changed. Safe there: behavior only, its own three constants stand alone, its
schema is `HEADERS.All_Program_Sessions` in `03` like every other tab's, and
everything it reaches for (`getSectionedRows`, `getPersistentFormRegistry`,
`registryKeyForSessionRow`, `extractFormId`, `syncRegistrations`) it reads at
CALL time or through a hoisted function declaration. `27` calls into it the
same way, in three places.

| File | | What is in it |
|---|--:|---|
| `99_registration_backfill.gs` | 591 | **The import walks the `Form_ID` COLUMN and nothing else** (`getDistinctFormIds`), and reads each form from `LAST_FORM_SYNC_TIME`. Put those two together and there is a failure with no output at all: a session row whose `Form_ID` names a form that has been DELETED, while the link cell beside it — the one staff hand out, the one filling with responses — names a form the import has never heard of. Nothing opens it, so nothing can report that it is going unread. That is how a rolling appointment form (`94`) came apart from its rows: `adoptAssistanceProgramSessions()` repoints `Form_ID` and both link cells together, but a row that took only half of that (`32`'s "a live link, on the right row, to the wrong form") points the importer at the old month form and the reader at the new one.<br><br>**And repairing the pointer does not bring anybody back**, which is the half that is easy to miss and the reason this file exists rather than a line in `32`: every response submitted while the pointer was wrong is behind the sync clock, so correcting `Form_ID` collects only what arrives afterwards — and the tab then looks healthy, which is worse than looking broken. The clock is only ever read and advanced (`27`), with no reset anywhere. So a form can now be MARKED (`REGISTRATION_BACKFILL_PROP_KEY`, versioned like every stored shape) and the ordinary sync reads that one from the epoch instead: `backfillSinceFor()` is the whole mechanism, and `pendingBackfillFormIds()` is UNIONed into the work list so a form no session row names — the only kind anybody reaches for this with — is reachable at all. **Not a second importer**, deliberately: the waitlist arithmetic against live occupancy, the recount, the dashboards, the leader sheets and the invitations are all downstream of the form loop, and a private copy of that here would be a second thing to keep in step forever. Re-reading a whole history is safe because `buildRegistrantRow()` already is — a response whose row exists is patched in place by `Party_ID`, a deleted registration stays deleted on its tombstone, a hand-edited row is untouchable. The mark is cleared on exactly the condition `LAST_FORM_SYNC_TIME` is advanced on, and for the same reason — the whole window read AND the rows on the tab — which is why the list of what has been read is carried on the sliced sync's PLAN (`98`) rather than on the execution: the slice that closes the window is usually not the slice that read the form, and a per-execution list would leave every mark uncleared and every marked form re-read forever. A form that can never be opened therefore keeps its mark and is named in the digest every run, which is the visible version of this problem rather than the silent one.<br><br>**The silence itself was the bug**, so the other half is observability. `describeUnimportedFormPointer()` is what `27`'s "could not read this form" note says now — which programs and what span of dates have stopped importing, that the workbook's own form registry names a DIFFERENT form for those sessions (free to check, and this fault's signature), and that the fix is two steps rather than one. `reportUnimportedForms()` is the standing read-only check for the shape nothing else looks for: a form holding responses that NO session row names. Both are on the Admin menu — the re-import beside the Form & Link Doctor, because it is the other half of the same repair; the report under Reports. `tests/registration_backfill.test.js` pins the read window, the marking, the three facts in the note, and that a form with no responses is not reported as a fault. |

### Every file this system made, opened back up (89)

Numbered into the gap left at `89` (the office digest is `88`, and `90` was never used) rather than after the highest file, because the numbers past `96` were taken while this was being written. Safe anywhere: behavior only, its three constants stand
alone, and everything it reaches for — the registries (`06`, `46`, `69`), the
folders (`82`), the Config reads (`15`) and `openUpFileToAnyoneWithLink` (`46`)
— it reads at CALL time or through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `89_open_up_all_sharing.gs` | 305 | **The sharing repair, over every artifact instead of only the forms.** Every file this project makes is created by whoever pressed the button and read every hour by whoever owns the triggers — routinely a different account — and Drive gives a new file to its creator alone, so the hourly run is refused and the work stops without anything looking broken: responses stop importing, a leader's ticks never come back, a sign-in Doc cannot be rebuilt, a form image will not load. `46`'s `openUpAllFormSharing()` already did this for FORMS; everything else was repaired only by the run that happened to touch it (and a registrant sheet only the FIRST time — `pushProgramLeaderSheets()`'s `accessOpened` flag), which is no help for a file made before that code existed or by an account that has left. `collectGeneratedArtifactTargets()` reads the same registries `organizeGeneratedFiles()` (`82`) files things by — deliberately, because two lists of "what this system made" drift and the one that drifts is the one that silently stops repairing something — plus the session table's `Form_ID` column BESIDE the stored form registry, since those disagree in both directions (a re-keyed group, a form repointed by hand). **Two kinds stop short of the link** and get the named editors only: a FOLDER, because a link-editable one hands over everything inside it now and in future, and a form IMAGE, because `55` promises whoever uploaded it that "a photo put on a public form is not a Drive file made public" — the question builder copies the BYTES onto the form (`54`), so the syncing account needs to read the blob and nothing else. `openUpFileToAnyoneWithLink` therefore takes two independent options: `folder` says how to FETCH (`DriveApp.getFileById()` THROWS on a folder id and would report every folder as unreachable) and `linkSharing` says how far to OPEN, defaulting to on for a file and off for a folder. **New files need none of this** — every creation path already opens its own file at birth (`26` a registration and lunch form, `05` the template, `45` a sign-in Doc, `46` a registrant sheet, `47`/`49` a rebuilt one), and the import repairs a form it cannot open mid-run (`27`); this sweep is for what was made before that or by an account that has left. It **carries itself on**, on `runSlicedJob` (`75`) like the four other long jobs here: three hundred artifacts is more Drive calls than one execution has, and the first version of this stopped at its deadline and asked somebody to press the item again — a repair that only finishes if a person sits there pressing it, on exactly the workbook where it matters most. The plan is fixed at the press (a file made by the hourly sync mid-sweep would otherwise extend a sweep trying to end, and is already opened up by the run that made it), each slice records every file as it finishes and arms the next thirty seconds out, and it holds NO workbook lock — every call is to Drive, so shutting the desk out of Quick Mark for four minutes would buy nothing. `OPEN_UP_SHARING_SWEEP_V2` is the state (`_V2` because the shape changed when it became a sliced job), stale after an hour so one dead sweep cannot block every later press; `cancelOpenUpAllFileSharing()` is the editor-only escape hatch. Every step is idempotent, which is what makes the resume safe rather than delicate. `tests/open_up_sharing.test.js` pins which artifacts are claimed, the de-duplication, the folder rule, that an unreadable registry costs its own entries only, and the hand-off: a slice out of budget records what it did, arms exactly one resume trigger, and the next slice finishes the rest and clears both. |

### Everything the desk could not say (99a)

Numbered `99a` rather than `99`: the re-import backfill above took that prefix
on `main` while this was being written, and renumbering an EXISTING file is the
one edit this project cannot make — so the newcomer moved, the same way `80`–`82`
did. The letter suffix follows `01a_lazy_globals.gs`, and it keeps the file
sorting where a reader looking for "the last one" expects it, which `100_` would
not. Safe at the end for the usual reason: behavior only, its own two constants
stand alone, its schema is `HEADERS.All_Registrants` in `03` like every other
tab's, and everything it reaches for (the cancellation writers in `71`, the
session lookups and the walk-in helpers in `38`, the tombstones in `28`, the
render in `35` and the two recounts in `30`/`44`) it reads at CALL time or
through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `99a_registrant_changes.gs` | 1178 | **Quick Mark could mark and add; it could not correct.** Its five ticks either record something that happened or put somebody ON a list, which left the other half of a front desk's day — "she rang to move to Thursday", "he's not coming", "she cancelled and now she can", "I marked the wrong Mary", "she wants the cold plate", "that number is two digits out", "he's on there twice" — reachable only on the Registrants tab, through the Move Sessions dialog, or by marking `Manual_Override` and running a menu sweep. None of them was reachable in front of the person they were about, which is the only moment anybody knows them. `applyRegistrantChangeFromDialog` is the one entry point behind the dialog's **Change this registration** panel, dispatching nine actions (`REGISTRANT_CHANGE_ACTIONS`) onto the row `pickRegistrantRowForChange()` resolves — the same narrowing `applyQuickMarkLocked()` does, applied to row ARRAYS because every change here either rewrites a whole row or removes one, and both end at `renderRegistrantsSheet()` with the full set in hand.<br><br>**A status is never one cell**, so every status change goes through `71`'s writers rather than a fourth copy of the four: cancel through `cancelRegistrantRowsLocked` (directly, not `cancelRegistrantRows` — this already holds the desk lock, the nesting `applyQuickMarkForHousehold` avoids for the same reason), waitlist through `stampRegistrantRowWaitlisted`, and **Put them back on** through either `stampRegistrantRowActive` or the ONE new writer here, `stampRegistrantRowUncancelled` — the reverse of a cancellation, which did not exist because until now nothing could ask for it. A capacity waitlisting is still never promoted out of turn (`wasWaitlistedByHand`), and nothing is given a place `registrantChangeSeatRefusal()` says is not there.<br><br>**The move is the one that needed a tombstone.** Rewriting a row's `Event_ID` leaves the form response behind it still pointing at the old one, so the next import would put the original row back beside the moved one — `recordRegistrantTombstones()` BEFORE the rewrite (while the rows still say where they came from) is what closes that, and `clearRegistrantTombstones()` on the destination is what stops a move onto a session somebody was once removed from being silently undone. The marks do not travel when the DATE changes (`clearRegistrantMarksOnRow` — "attended" is a fact about a day), the meal follows the date and is dropped when the new one serves none, a guest goes with the person who brought them, and a move onto a session they are already on is refused as the duplicate it would be.<br><br>**`remove` deletes rather than marks**, which is the one place this parts company with `83`: that file's "a mis-click in a dropdown must not be permanent" is right about a dropdown, and this is nine changes deep behind a name, a date and a confirmation — at which point "it will go when somebody runs a menu item" is the desk being told a thing happened that has not. Same two writes `83` makes, in the same order, with the form response left exactly where it is. `tests/registrant_changes.test.js` pins the five rewritten cells, the tombstone's ORDER, the three move refusals, both halves of the restore rule, the cancel, the undo, the meal being SET rather than added, the contact spread across upcoming rows only, and the ask-then-delete.<br><br>All nine actions now append to the ledger, one entry per person rather than per press (a guest is a registration of their own): `move` carries `Payload.from` and the session columns as they LANDED, `remove` appends `removed` alongside the tombstone rather than instead of it (that swap is phase 5, not this one), and the contact correction appends one entry per upcoming row it touches, because eleven rows corrected in one press are eleven registrations whose details moved. |

### Nothing leaves the building at night (99c)

Numbered after `99b` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its two constants stand alone, it reads
`TIMEZONE` at CALL time rather than at load time, and its three callers (`15`,
`76`, `88`) reach it through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `99c_quiet_hours.gs` | 100 | **One window, fixed, and every send checked against it.** An hourly sync does not know what time it is for the person on the other end, so a roster alert at 11pm and a reminder at 6am were both ordinary. `isWithinMailQuietHours()` closes 5pm–8am (`MAIL_QUIET_HOURS_START_HOUR` / `MAIL_QUIET_HOURS_END_HOUR`, read in the WORKBOOK's timezone, half-open at both ends so a trigger firing on the hour has one answer), and it is deliberately NOT a Config cell: a house rule, not a setting somebody has to find, and a second switch beside `Pause_Outbound_Mail` is one more thing to read before answering "why did nothing go out?".<br><br>**Held, not dropped** — the opposite of what the pause does (`76`). `recordSent()` is not called, so the caller's ledger stays put and the first pass after 8am sends it; it comes back as the `'held'` status `66` and `70` already have for a message the quota could not afford, which is why neither needed a new branch. The office's daily digest (`88`) keeps its spool the same way. The one exception is `notifyAdminUrgent` (`15`), which has no ledger and no next pass: inside the window it is FILED for the 10am digest instead of sent, because nobody is at the desk at 2am to act on it and losing it would be the only other answer. `tests/office_copy_and_quiet_hours.test.js` pins both ends of the window, the hold and the untouched ledger; the five other mail tests pin `'H'` to midday so their answers do not depend on when they are run. |

### Which responses never became rows (99d)

Numbered after `99c` for the usual reason — never renumber, and this landed
last; `97` was taken by the render-batching file on `main`, and the prefix is
all that changed. Safe there: behavior only, its own two constants stand alone, its schema
is `HEADERS.All_Registrants` in `03` like every other tab's, and everything it
calls — `buildRegistryIndex`, `processFormResponse`, `getSectionedRows`, the
tombstone readers — is a hoisted function.

| File | | What is in it |
|---|--:|---|
| `99d_registration_import_audit.gs` | 791 | **A registration that was never imported leaves no trace, and that is the whole problem.** Somebody says a name is missing; the row is not on `All_Registrants`, the session is on the dashboard, the form still opens, and the hour's log is gone — and "never registered" and "dropped by the import" produce the identical silence. `auditRegistrationImport()` re-reads every response every form still holds, re-derives the rows they *should* produce, and names the people that comparison is missing.<br><br>**A response that cannot be RE-READ is not a lost seat, and that split leads the report.** The v8→v9 meal swap deletes `LUNCH_GRID` / `ALL_DATES_LUNCH_PEOPLE` / `EXTRA_MEALS` (and the legacy people-grid on a lunch-only form) and adds the counting questions in their place, so re-deriving a pre-v9 response today produces nothing — `readMealCountGridResponse()`'s fallback to the old titles resolves through `formIndex.byTitle`, which is items STILL ON the form, and the migration has just removed them. That is harmless if the response was read when it ARRIVED, which is why `convertFormToMealTotals` is sequenced after the import loop: the rows are on `All_Registrants` and nobody lost anything. So `buildNamesOnFormIndex_` asks the only question that separates the two — is this person on the tab for any session this form covers — keyed on the FORM rather than the session, because the dates are the part that did not survive. The report then says how many are harmless and names only the rest — **and names no cause for them at all**, which is the third correction this file has taken. It said the meal swap; the run that followed showed those responses answering `How many guests are you bringing?`, `How would you like to sign up?`, `Allergies` and `Anything Else?` and reading back perfectly, so nothing had been deleted from under them. What a response ANSWERED is checkable; what happened to the form is not, from here. So the evidence is printed and the conclusion is left to a person — including the mode question's VALUE, read rather than ticked off, because that answer is the fork the rest of the form hangs off (one branch is a single meal total, the other the date grid) and "it was answered" leaves both open.<br><br>**The placeholder row is a FINDING, not a skip** — the worst line this file ever had. `auditFormGridRows_` passed over `TEMPLATE_GRID_PLACEHOLDER_ROW` as "a form waiting for the next sync", which is true of a form built ten seconds ago and false of every other form carrying it: `31`'s own banner calls that state "a form nobody can register on", and it has happened here before. A live form whose date question offers nothing real takes submissions all day and records a name against no date. Worse, it does not heal — `applyFormDateLabels()` is fingerprinted and stores the fingerprint whenever the write did not throw, and `setGridItemRows` writes nothing at all when `findRosterGridItems()` matches no item, so a form can hold a fingerprint saying the dates were written over a grid that never received them and never be opened again. `auditFormGridHealth_` reports those above everything else, with the response count beside each, because it is the only finding here still losing registrations while somebody reads the report.<br><br>**Why a response derived nothing is ASKED, not assumed** — and that is a correction, not a design note. The file was written from a hypothesis (the v8→v9 meal swap, `makeFormLunchOnly()`) and its first report stated that hypothesis as the CAUSE of every empty response it found, having checked no such thing. The first real run put 50 responses under that heading across three forms, and the commonest cause needed no grid at all: a form NO session row names, whose every response derives nothing because there is no date to match against — which is `99`'s fault, with `99`'s two-step fix. `classifyEmptyResponse_` replaced the assertion with two cheap facts (how many sessions name this form; how many questions the response answered that the form still carries) and three named states — `noSessions`, `unreadable`, `answeredNoRows` — reported apart because they do not share a fix. The classifier keeps the answered item TITLES rather than a count, and the report FOLDS them: thirty-two people who all answered the same four questions and stopped are one investigation, not thirty-two, and the question missing from that list is the one none of them ever reached, grouped BY FORM because they cluster, and with the middle one called a place to LOOK rather than a verdict, since a genuinely blank submission is identical from here. **And the tombstone lookup is neutralized alongside the tombstone WRITE**, for a reason that is about evidence rather than safety: `buildRegistrantRow` returns null for a tombstoned row, so a response whose rows were all deliberately deleted derived nothing and was reported as one the parser could not read. Deriving every row and classifying it against the store directly is what keeps "somebody deleted this" and "this could not be read" apart.<br><br>**Two faults in the grid path, both silent.** `getGridResponseByTitle` (`28`) resolves a grid through `formIndex.byTitle` — items CURRENTLY on the form — so a response submitted against a question that was DELETED rather than edited (the v8→v9 meal swap in `68`, and `makeFormLunchOnly()` every time it shapes a lunch-only form) has nothing left to resolve against: `processFormResponse` hits `if (!attendanceGrid && !mealGrid) return []` and imports nobody, with no warning, because an empty return is also what a blank submission legitimately does. And the same function returns the grid's rows from the LIVE item while the answers are as SUBMITTED, which `processFormResponse` then zips BY INDEX (`values[rowIdx]`) — so a date row inserted mid-month shifts every row below it, answers land on the wrong date, and the tail runs off the end of `values` and is dropped. **A size mismatch is the half that can be PROVED** (`auditResponseGridShape_`); a reorder that kept the row count is misread identically and is invisible through the Forms API, which is why the report calls its count a floor rather than a total and why `tests/registration_import_audit.test.js` pins that wording. `auditFormGridRows_` is the third check and the cheapest: a live grid row resolving to no session is one nobody on it is being imported from, and it is a property of the FORM, so it is asked once per form rather than once per response.<br><br>**It repairs nothing, deliberately** — the repair for a misaligned grid is a judgement about which date a person meant, and this file has no standing to make it. **And the read-only guarantee is code, not a promise**, because `processFormResponse` is written for the live import and has three side effects: `buildRegistrantRow` MUTATES a matching existing row in place (answered by handing it an empty `existingRowIndex` and empty `protectedKeys`, which the comparison wants anyway), `saveAllDatesRegistryEntry` sets a dirty flag (answered by never calling `flushPersistentRegistries()` and restoring the flags found on the way in), and `clearRegistrantTombstones` writes to Script Properties IMMEDIATELY — the one not-flushing cannot cover, so `withReadOnlyRegistries_()` swaps it for a recorder and restores it in a `finally`, including when a form throws. What it would have revived is reported instead, since a deleted registration a re-read would put back is a finding of its own. Menu: 🔧 Admin ▸ 📄 Reports, ungated like the two reports beside it — the person who noticed the missing name is the person who should be able to press it. Bounded by `REGISTRATION_AUDIT_BUDGET_MS` and says how far it got rather than dying mid-loop.<br><br>`withReadOnlyRegistries_()` now neutralizes a FOURTH side effect: `buildRegistrantRow()` composes a ledger entry per row and `supersedeRegistrantRow()` appends one (`99k`). Swapped for no-ops by the same mechanism and dropped rather than merely not flushed, because the mint path appends straight into the ledger's own buffer and the ledger is append-only — an entry written by a re-derivation could not be taken back out. |

### Volunteers, and the hours they work (99e)

Numbered after `99d` for the usual reason — never renumber, and this landed
last. Safe there: behavior plus its own vocabulary, its schema
(`SHEET_NAMES.VOLUNTEER_HOURS`, `HEADERS.Volunteer_Hours`,
`VOLUNTEER_HOURS_STAFF_COLUMNS`) lives in `03` like every other tab's, and
everything it reaches for — `writeMemoryTab` (`40`), `collectKnownMembers`
(`38`), `listKnownProgramTitles` (`53`) — it reads at CALL time or through a
hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `99e_volunteer_hours.gs` | 694 | **A volunteer is the one person in the building who is not attending anything.** They are working, they stay for a session they are not in, and a good many of them are never at either building — the private counselling happens elsewhere and the Mah Jongg leaders telephone round from home. `Person_Type` on `All_Registrants` would have covered the first kind and nothing else, because every row there hangs off an `Event_ID` and there is no calendar event for an afternoon on the telephone. So `Volunteer_Hours` is a tab of its own, **one row per VISIT** — the same person leading the same class every Tuesday is thirty-six rows and thirty-six sets of hours, which is the grain that sums any way the year-end return asks: by person, by program, by month.<br><br>**The hours are worked out where they can be and typed where they cannot.** `volunteerVisitHours()` reads Arrived and Departed (`parseVolunteerClockTime` takes a Date, "9:30 AM", "14:15" and a bare hour) and the times BEAT a typed number, because they are the evidence and it is a summary — a departure corrected at the desk has to correct the total. Its one refusal is a departure at or before the arrival: that is somebody typing 9 for 9pm, and reading it as a negative afternoon would put a wrong number into a figure the centre is credited on, so it falls back to the typed hours and `describeVolunteerHoursProblem()` says so. A volunteer working from home has no times at all and their typed hours are never overwritten.<br><br>**Nothing derives a row here**, which is what makes the tab safe to hand-edit and safe to delete: no sync writes it, and the only thing that adds a row is somebody pressing a button — `showVolunteerHoursDialog()` from Rosters & Sharing, or the link at the foot of Quick Mark, which is where the desk already is when a volunteer walks in. `writeVolunteerHoursTab()` is the ONE writer, newest first (it is a log — the row somebody wants is the one they just added) and it recomputes every Hours cell on the way through. `summarizeVolunteerHours()` and `reportVolunteerHours()` are the annual figure by person, ungated like the reports beside them; `volunteerMetricsForMonth()` is what `83` stores. `tests/volunteer_hours.test.js` pins the clock, both fallbacks, the backwards pair, what counts as a row, people-once-visits-every-time, and that nothing from the workbook is written into the dialog as markup. |

### The volunteer's own hours page (99f)

Numbered after `99e` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own four constants stand alone, its schema
is `HEADERS.Volunteer_Hours` in `03` like every other tab's, and everything it
reaches for (`logVolunteerVisit` and the clock helpers in `99e`, the PIN and
location helpers in `60`, `listKnownProgramTitles` in `53`, `spoolOfficeNote`
in `88`) it reads at CALL time or through a hoisted function declaration.
`60`'s `DOOR_ROUTES` gains a `volunteer` entry that names it, which is lazy for
exactly this reason.

| File | | What is in it |
|---|--:|---|
| `99f_volunteer_self_log.gs` | 430 | **`99e` records a volunteer visit from a modal dialog inside the spreadsheet, and every volunteer that tab is about is the one kind of person who cannot open it.** The hardest hours to capture are the ones worked away from both buildings — the support call taken at home on a Saturday, the Mah Jongg leader ringing round — and the only route left was to email the office and hope somebody retyped it, which is two people's time to record one person's and which stops happening in about a month. So this is the same write on a URL: `?mode=volunteer` on the SAME deployment the door pages are served from (`VOLUNTEER_SELF_LOG_MODES` spells it four ways, because the address is pasted into an email and typed off a phone), writing through `logVolunteerVisit()` — the tab's one writer, same desk lock, same arithmetic, same refusals. Nothing here derives an hour or a row of its own.<br><br>**It names nobody.** The staff dialog offers `collectKnownMembers()` in a dropdown, which is right in front of staff and wrong on a link that leaves the building; the volunteer types their own name, and `volunteerSelfLogContext()` carries only the roles, the buildings, the program TITLES (already on the public calendar, `86`) and today's date — a field list `tests/volunteer_self_log.test.js` pins as the whole of what may leave. It defaults to Off-site and to today, and takes a plain number of hours as readily as a pair of times, because the visit it exists for has no arrival time: nobody watched it start.<br><br>**The refusals it owns are the ones only a public page has**: the PIN (the same `60` PIN — this route writes, so it is gated like everything else in that table, and the Door Pages dialog's new link row says to set one before sending the address out), a name, a date that is neither in the future nor more than `VOLUNTEER_SELF_LOG_MAX_BACKDATE_DAYS` back, and `VOLUNTEER_SELF_LOG_MAX_HOURS` — the typo guard, because "30" typed into a box labelled hours when the answer was thirty minutes is the mistake this page invites and a 30-hour visit in a figure the centre is credited on is worse than a refusal.<br><br>**Attribution is the one thing the page cannot know.** A web app runs as the workbook's OWNER, so `getCurrentUserEmail()` would sign every self-logged row with the office's address and the tab would read as the office having typed them all in. `logVolunteerVisit()` therefore takes an explicit `loggedBy` (its only change), and this page passes the volunteer's own address marked `(self-logged)`, with `Self-logged by …` on the row's note: a signature on a timesheet, not an identity claim. And the office still hears — one line per entry in the daily digest (`88`), which is the whole of what replaces the email this page exists instead of. |

### Why did nothing happen? (99g)

Numbered after `99f` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, `BACKGROUND_JOB_STATES` is lazy (`01a`, since
every key it names lives in another file), its other two constants stand alone,
it declares no schema, and everything it reaches for — the four sliced jobs'
state keys (`25`, `49`, `90`, `98`), the Config reads (`15`), the escape
hatches (`25`, `49`) — it reads at CALL time or through a hoisted function
declaration.

| File | | What is in it |
|---|--:|---|
| `99g_nothing_happened.gs` | 300 | **The fault this file is about is not a fault — it is a REFUSAL nobody can see.** Half the menu here begins by asking whether it is allowed to run: automation may be paused (`automationGateAllows`), a large-setup import or a destroy-and-rebuild sweep may be in flight (`isBootstrapActive` / `isDeskWorkBlocked`). Each declined with a TOAST and returned — and a toast is the one thing nobody sees, because it lands in the corner of a sheet that is at that moment showing Google's own "Running script…" banner and is gone before anybody looks up from the queue. What that adds up to at the desk is the worst possible symptom: several unrelated menu items that "show running script and then nothing happens, no error", with the executions list reporting every one of them as **completed** — which is the truth, because the function ran, declined and returned.<br><br>**And the refusals compound.** A sweep that paused the hourly triggers and then died without finishing (an execution killed at the account's ceiling reaches no `finally`) leaves every gated item declining, hourly, indefinitely, while the registrations quietly stop importing and the workbook goes on looking fine.<br><br>So: `reportWhyNothingHappened()` is one ungated, read-only menu item — directly under **Update Everything Now**, because it is what somebody reaches for about the item above it — that states in an ALERT every reason the workbook currently has for refusing, **all of them, not the first** (a paused workbook mid-sweep has two, and fixing one and finding nothing changed is the same dead end again). Every check is guarded on its own, because a report that throws is a report nobody can read and on this workbook something throwing is exactly what is being asked about. `clearStuckBackgroundJobs()` is the escape hatch moved out of the Apps Script editor — it prefers each job's own `cancel` (those put back what the job paused; dropping the state alone would leave a workbook that never syncs again and nothing to say why), and a job still advancing is reported rather than accused. And `explainRefusal()` is how the gated entry points say no now: an alert where there is a UI, the toast where there is not, which is every trigger run. `tests/nothing_happened.test.js` pins all of it. Beside it, `QUICK_MARK_INLINE_INDEX_MAX_CHARS` (`36`) is the same failure in the other direction: a modal served as one huge HTML document does not fail loudly — the execution completes and no dialog appears — so an index too big to carry is fetched by the page instead. |

### Why is a roster sheet empty? (99h)

Numbered after `99g` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its one constant is derived from nothing, its
schema is `HEADERS` in `03` like every other tab's, and everything it reaches
for (`46`'s registry, join, fingerprint and stranded count; `34`'s reader) it
reads at CALL time or through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `99h_leader_sheet_doctor.gs` | 210 | **The sentence this file exists for is "Nobody has signed up yet."** A program registrant sheet says exactly that when the roster handed to it is empty — true of a class nobody booked, and false in the same words of a class with a dozen people on the Registrants tab whose rows never reached it. From the sheet the two are identical; from the workbook they are four faults with four fixes, and telling them apart meant reading `pushProgramLeaderSheets()` and working out which branch a program took on a run nobody watched. `diagnoseLeaderSheetRosters()` walks the push's own path on the push's own inputs — the same `buildLeaderSheetRowsByProgram()`, never a second implementation of the join, because a diagnostic that computes the answer its own way can only tell you about itself — and reports each step: whether the program is in the registry and under which key, how many SESSION rows that key has inside the sheet's window, how many registrant rows joined, how many name the program and joined to nothing (the Event_ID drift `46`'s fallback heals, and this is how you see that it happened), whether the stored fingerprint matches what the rows would produce (the one state where the push deliberately writes nothing), and whether the file still OPENS — the one answer not knowable from the workbook, and the one that has been silently stale longest. Read-only, no lock, ungated, under 🔧 Admin ▸ 📄 Reports: the person staring at an empty roster is the person who should be able to press it. Every line carries the sheet's own LINK, because "the sheet is empty" and "the sheet I am looking at is empty" are the same sentence about two different files — and the report leads with `findDuplicateLeaderSheetPrograms_()`, TWO REGISTRY ENTRIES FOR ONE PROGRAM, which is the fault that looks healthy from both ends: the push writes the roster to one file and stamps the other's refresh note, so the workbook reports rows written while the leader's link opens "Nobody has signed up yet", and neither side is lying. Two more things it now refuses to take on trust. It READS the tab's first data cell, because the fingerprint is a claim about a file this workbook cannot see: a program whose rows joined and whose sheet is showing the placeholder anyway leads the whole report, where it used to be printed in the healthy section as "already written, the next push will skip it" — true about the fingerprint, and the single most misleading sentence it could offer somebody staring at the empty sheet. And `findSharedLeaderSheetFiles_()` groups on the FILE, not the program name: two entries with different names pointing at one spreadsheet both write the same tab in the same pass, so the last one reached decides what is on it while the other reports rows nobody can see — which the name-based twin check above cannot see at all. `tests/leader_sheet_doctor.test.js` pins that the four cases produce four different sentences, the twin case its own, the contradiction leading the report, a healthy sheet not being accused of it, and the shared file. |

### What would we say, said to us instead (99i)

Numbered after `99h` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own four constants stand alone, it
declares no schema, and everything it reaches for (`isNotificationTestMode` /
`getAllAdminNotificationEmails` in `15`, `CONFIG_LAYOUT` in `04`,
`spoolOfficeNote` in `88`) it reads at CALL time or through a hoisted function
declaration. Its two callers — `76`'s one send and `33`'s guest list — reach it
the same way.

| File | | What is in it |
|---|--:|---|
| `99i_notification_test_mode.gs` | 250 | **The third switch, and the question the other two cannot answer: "what would this workbook say to people if I let it?"** `Pause_Outbound_Mail` stops the mail and DISCARDS it, so all you learn is that nothing went out; `Automation_Enabled` stops the syncs, so there is nothing to look at at all. Neither shows you the reminder a member would have read, in the words they would have read it in, addressed to the address it was going to. `Divert_Mail_To_Admin` on Config (`CONFIG_LAYOUT.TEST_MAIL`, `04`, fresh columns like every section after `ADMIN_NOTIFICATIONS` and for the same never-reflow reason) readdresses every outgoing message to the office's own Admin Notification Emails table — **every address in it, ticked or not**, like the daily digest, because the per-category ticks answer a different question — with the real To / Cc / Bcc, the real subject, who it was about and what sent it printed in a block above the body, and the message itself **byte-for-byte underneath**: a rehearsal that paraphrases is a rehearsal of the paraphrase.<br><br>**Three rules, each a deliberate difference from the pause.** (1) `recordSent()` is NOT called — the pause calls it on purpose so its churn never arrives late, and a test must leave the member's real reminder owed, so turning the switch off delivers it. (2) BECAUSE of (1) the same messages divert again next pass, so it is capped at `NOTIFICATION_TEST_MODE_MAX_PER_RUN` (12, because the point is to READ them) and the overflow is one line saying what was not seen. (3) **Calendar invitations are held too**, which the pause does not do: Google emails a guest the moment they are added, so `33` calls `recordHeldCalendarInvite()` instead of `addGuest()` — the guest list is left untouched rather than added-and-removed, because an invitation withdrawn a second later is two emails instead of none.<br><br>**A diverted message answers `'held'`** rather than a status of its own, which is the interesting decision: every caller in `66` and `70` already knows held means "not sent, not recorded, tried again next pass", and a new status would fall through their three branches into the "could not send" warning — a mode whose whole promise is that nothing went wrong, printing a warning per message. The switch also files its own "this is ON" line once per execution (`88`), and `99g`'s report lists it as a reason nothing reached anybody: the syncs run, the log says messages went, and no member heard a thing. `tests/notification_test_mode.test.js` pins the office address, the block, the untouched body, the ledger left alone, the cap, that the pause still wins, that quiet hours do not hold a rehearsal, the no-addresses case and the held invitation. |

### The registrant tab's safety net (99j)

Numbered `99j` on the `01a`/`99a` precedent — every prefix through `99i` is
taken, and renumbering an existing file is the one edit this project cannot
make, so the newcomer moved. Safe at the end for
the usual reason: behavior only, its four constants stand alone, its schema is
`SHEET_NAMES.REGISTRANT_DASH` in `03` like every other tab's, and everything it
reaches for (`getSectionedRows`, `spoolOfficeNote`, `notifyAdminUrgent`,
`getOrCreateSystemFolder`, `TIMEZONE`) it reads at CALL time or through a
hoisted function declaration. Two earlier files call into it the same way: `34`
(the guard, immediately before its `clear()`) and `16` (the nightly trigger and
the menu item).

| File | | What is in it |
|---|--:|---|
| `99j_registrant_safety_net.gs` | 216 | **`All_Registrants` is rebuilt by reading ITSELF, clearing itself, and writing the rows back** (`renderRegistrantsSheet` → `getSectionedRows` → `sheet.clear()` → `writeUpcomingPastSections`), which puts every registration in the building on one in-memory array being complete at the instant of that `clear()`. Anything that makes the array short — a partial read, an over-broad tombstone match, a filter that threw half way, an upstream pass handing on fewer rows than it was given — is silent, total and permanent, and there is no second copy of this tab anywhere in the project. Every recovery so far has been out of Google's own file version history.<br><br>This does not fix that shape; it makes it survivable. **`guardRegistrantRowLoss_()` is consulted immediately before the `clear()`** — before `invalidateSectionedRowsCache()` too, so its count is the memoized read the render already paid for — and only for a tab whose caller declared `opts.guardMarker`, which is `All_Registrants` and nothing else (triage and `Lunch_Schedule` are projections of the calendar and are rebuilt from it). **Both conditions are required for a refusal** and that pairing is the whole calibration: the FRACTION (`REGISTRANT_GUARD_REFUSE_FRACTION`, half the tab) is what makes it impossible to hit by legitimate means, since nothing on any menu removes half the registrations through a render; the ROW FLOOR (`REGISTRANT_GUARD_REFUSE_MIN_ROWS`) is what keeps a four-row tab from refusing to redraw when two of them are correctly dropped. A refusal **throws** rather than returning quietly — a silent skip is the failure mode that produced this file, the sliced sync (`98`) is built to record a step that threw and step over it, and a desk action that refuses in front of somebody is the correct outcome. A smaller loss (`REGISTRANT_GUARD_WARN_MIN_ROWS`) is written but snapshotted and spooled for the digest, because legitimate ones exist — the duplicate merge (`85`), the remove sweep (`83`), a session deletion (`48`) — and a guard that blocks those is a guard somebody turns off.<br><br>**And the second copy**: `snapshotRegistrantTab_()` writes the tab as a dated CSV into a `Registrant Snapshots` folder (through `82`'s anchor, like every other folder here), on the nightly `snapshotRegistrantsDaily` trigger at 3am — ahead of the 5am calendar sync, so the copy is of the day that ended — from the menu (`snapshotRegistrantsNow`), and on every guard firing. Values rather than formulas, deliberately: it is a record to read a lost registration back out of, not a tab to restore wholesale. Pruned after `REGISTRANT_SNAPSHOT_KEEP_DAYS` and TRASHED rather than deleted. It never throws: a snapshot that cannot be written must not be the reason a render fails, and the refusal's message says whether there is one.<br><br>**Deliberately not a ledger.** The durable answer is an append-only record every writer appends to FIRST, with this tab as a fold of it — at which point the tombstones (`28`), `Manual_Override`'s protection role and both halves of this file stop being load-bearing. That is a much larger change; this is what holds the line until then, and is worth keeping afterwards regardless, because a guard on a destructive write costs one comparison. `tests/registrant_shrink_guard.test.js` pins both directions of the calibration, that the snapshot is taken BEFORE the throw, that a healthy render is silent and never reads the tab, and that a tab with no marker is untouched. |

### The second copy of every registration (99k)

Numbered after `99j` for the usual reason — never renumber, and this landed
last. Safe there: its own five constants stand alone (`LEDGER_KINDS`,
`LEDGER_ENTRY_KINDS`, `LEDGER_SOURCES`, `LEDGER_ENTRY_SOURCES`,
`LEDGER_PAYLOAD_RESERVED_KEYS`, `LEDGER_DEAD_STATUSES` — vocabulary, derived
from nothing), its schema (`SHEET_NAMES.REGISTRATION_LEDGER`,
`HEADERS.Registration_Ledger`) lives in `03` like every other tab's, and
everything it reaches for — `getOrCreateSheet` (`07`), `getIndexMap` (`03`),
`registrantTombstoneKey` (`28`), the four status stampers (`71`, `99a`),
`clearRegistrantMarksOnRow` (`99a`) — it reads at CALL time or through a
hoisted function declaration. Two earlier files call into it the same way:
`06` (`flushPersistentRegistries`, which flushes the buffer) and `75` (the
sliced runner's `finally`).

| File | | What is in it |
|---|--:|---|
| `99k_registration_ledger.gs` | 1101 | **The append-only record `99j`'s banner says is the durable answer.** `All_Registrants` is rebuilt by reading itself, so at the instant of the `clear()` one short in-memory array is the only copy of every registration this centre holds; `99j` is the smoke alarm and its own banner is honest that it cannot see the loss of nine rows and cannot say WHICH rows went, because nothing in the workbook knows what the tab was supposed to say. This is what knows.<br><br>**One row per EVENT, not per person** — that is the whole difference. The tab is one row per person per session MUTATED IN PLACE, so "Joan cancelled" is the absence of a word she used to have and "Joan moved to Thursday" is a row that has always said Thursday. The ledger records the happening, three rows where the tab has one, never edited and never removed — and what the tab cannot hold at all (that she rang on the 15th, that the cancel came through the link in her calendar invite) is on the record for the first time. **No kind is ever deleted or rewritten**: a mistaken entry is answered by a further entry, which is what `removed` is for, and the fold's job is to make the last word win.<br><br>**Nine kinds** (`LEDGER_KINDS`), of which two are not in anybody's first list and neither is optional: `superseded`, because a newer submission replacing an older one is an existing path (`29`) and a replay that did not know about it would put both seats back; and `reactivated`, the reverse of the two reversible states, which is `71`'s `stampRegistrantRowActive` and `99a`'s `stampRegistrantRowUncancelled`. `removed` is NOT a cancellation — a cancellation is a fact about a person, a removal is a fact about a mistake. `LEDGER_SOURCES` is one value per CALL SITE, so "where did this come from" is read rather than inferred.<br><br>**`Registration_ID` is the fold key and it is deliberately not `Party_ID`**: a party is a household submitting together (four rows, one id) and it is blank on everything this system writes outside the form import. A `registered` entry is the only kind that may MINT one, which is what makes "this registration exists" something the ledger SAYS rather than something a reader infers; `resolveRegistrationId()` is the second path, through the same `registrantTombstoneKey` (`28`) the tombstones, the superseded match and the import index are already built on, and a key resolving to nothing is the correct reading of "this person is on a roster and the ledger has never heard of them" rather than a fault.<br><br>**`Payload` is the fields an entry SETS and no others, never a whole row** — a whole row per entry would make every entry a fresh assertion of every field, and a stale field in a `corrected` entry would then silently undo a change made between the read and the append, which is the same class of fault this tab exists to remove. A payload key that is not a column of `All_Registrants` is REPORTED, because a writer appending `Attending` where the column is `Attended` is a correction that never lands and is silent in every other way.<br><br>**The buffer, and the two `finally`s.** `sheet.appendRow()` per entry is a round trip per entry and the import cannot afford it, so `appendLedgerEntries()` buffers and `flushLedger()` writes once past the last row — `getLastRow()` asked of the sheet every flush rather than remembered, because two overlapping syncs are ordinary here. **An entry buffered and never flushed is the original loss in a new coat**, so the flush is called from `flushPersistentRegistries()` itself (one call site rather than twenty that can be forgotten separately) and from a `finally` in `runOneSlice_` (`75`), which every sliced job in the project passes through. A flush that throws leaves its entries PENDING for the next one, and `__ledgerWrittenIds` is what stops that retry doubling a `registered` entry — which would be a doubled seat. It takes **no new lock**: every phase 2 caller already holds the workbook lock and `LockService` locks are not reentrant (`99b`'s banner, on exactly this).<br><br>**`foldRegistrationLedger(entries)` is the replay**, and its output is one row per live registration in `HEADERS.All_Registrants` order — which is exactly the array `renderRegistrantsSheet(force, allRows)` already takes as its second argument, so phase 4 is a new SOURCE for an existing writer rather than a new writer. The statuses go through `71`'s own stampers rather than a fourth copy of the four cells they write, which is what keeps "a status is never one cell" true by construction. A second `registered` on a LIVE id is a reactivation plus a correction and never a second seat (they cancelled and signed up again is the ordinary case); a move clears the marks, because "attended" is a fact about a day; `removed`, `superseded` and an absorbed `merged` are dead and are not resurrected by a later entry. Everything the replay cannot honour goes into `problems` and nothing is thrown — one bad entry must not cost the replay the ones after it, and a fold that refuses to produce rows is a tab that cannot be drawn.<br><br>**And since phase 2, how a writer finds the id it is acting on** — the half of §1.4 that phase 1 only declared. `ledgerIdForRegistrantRow(row, map)` reads the `Registration_ID` column where phase 3 has put one on the row, resolves through `ledgerFoldNow()`'s index (one read of the ledger, folded, memoized for the execution and dropped by `flushLedger()`) where it has not, and MINTS where the ledger has never heard of this person — which before the backfill is every row in the workbook. **What a minted entry carries is the ROW**, through the same `ledgerEntriesForExistingRow()` the backfill uses, because a bare `registered` entry would fold to a name and a session and nothing else and the verifier would then report every column of a healthy registration as a disagreement. A row that is not Active gets its `registered` entry PLUS the one entry that puts it in that state, so the replay reproduces the tab rather than reviving everybody.<br><br>**`ledgerEntryForStatusChange_()` is the one composer the four status writers share, and the append deliberately does NOT live in `71`'s stampers** — which is where it would otherwise obviously belong. The fold calls those stampers too, against its in-progress state, so a stamper that appended would write the history it was replaying back into the ledger, doubled, every time anybody folded. So the callers compose, and they compose BEFORE they stamp and append only if the stamper returned true: composition is where a vocabulary error throws (with the row untouched), while a stamper's refusal — already cancelled, not waitlisted by hand — is an answer rather than a failure, and an entry for a change that did not happen is a seat given back twice by the replay. `ledgerCellValue_()` is what every Payload in the project is built through, so four files cannot disagree about what a date looks like in an entry (`yyyy-MM-dd`, never a `Date`, and the check is `Object.prototype.toString` rather than `instanceof` because a value read out of a leader's shared sheet is a `Date` from another realm).<br><br>**The import composes but does not append** (`recordImportLedgerEntries` / `takeImportLedgerEntries`), and the reason is ordering rather than round trips: `appendLedgerEntries()` buffers, so a direct append would cost nothing per response — but a row `buildRegistrantRow()` builds is not a row anybody has written yet, and an entry in the ledger's own buffer would be flushed by `75`'s `finally` whether or not the render ever happened. `27` hands the batch over inside the same `sync.step` that writes the tab, so the appends go with the rows and a slice that ran out of budget records exactly what it wrote.<br><br>`ledgerRegistrationIdsByKey()` is the one resolver that answers over DEAD registrations too, for the backfill (`99o`) and nothing else — handing a writer the id of a registration that was removed, superseded or absorbed would have the fold drop the entry it then appends, silently, which is the worst answer a resolver can give.<br><br>**Phases 1–3 of `REGISTRATION_LEDGER_DESIGN.md` are shipped; phases 4 and 5 are not, and the gate is deliberate.** The tab is still the state, nothing renders from the replay, and no caller of `renderRegistrantsSheet()` has stopped passing its own array — that is phase 4, gated on a month of clean verifier runs (`99n`) and on `18`'s `onEdit` appender proving itself in production, because it is the only phase of this design that can lose anything. `28`'s tombstones, `Manual_Override`'s protection role and `99j`'s thresholds all stand exactly as they were. `tests/registration_ledger.test.js` pins the vocabulary's two throws, the buffer (one write, past the header, pending after a refusal, never doubled), the replay (the four cells of a cancellation, the reactivation that is not a second seat, the marks that do not travel, the unknown payload key, all three ways a registration dies and that none of them comes back), and phase 2's resolution: the known row, the minted one, and the same id twice before a flush. |

### Is the ledger telling the truth? (99n)

Numbered after `99m` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own three constants stand alone, its
schema is `HEADERS.All_Registrants` in `03` like every other tab's, and
everything it reaches for — the fold and the ledger's readers (`99k`),
`getSectionedRows` (`34`), `spoolOfficeNote` (`88`) — it reads at CALL time or
through a hoisted function declaration. One earlier file calls into it the same
way: `98`, as the last step of the sync's tail.

| File | | What is in it |
|---|--:|---|
| `99n_ledger_verifier.gs` | 348 | **What makes phase 4 safe to take LATER is this check, run now.** Phase 2 put an append in front of nine writers and nothing reads the result — the tab is still the state — so the only thing that can say whether those nine are actually appending is a comparison. `verifyLedgerAgainstTab()` folds the ledger, reads the tab, and matches on `registrantTombstoneKey` (`28`), which is the key the tombstones, the superseded match, the import index and the fold's own index are all already built on; a fifth spelling of "the same registration" is how two readers come to disagree about what one is.<br><br>**Three buckets, reported APART, because the fix for each is a different fix.** *On the tab, not in the fold* — a writer that is not appending; before the backfill that is every row written before the ledger existed, and afterwards it is a call site somebody has to go and find. *In the fold, not on the tab* — **the original fault, detected for the first time**, and what this phase is worth shipping for on its own: `99j`'s guard cannot see the loss of nine rows and neither half of it can say WHICH rows went, because neither knows what the tab was supposed to say. This names the person and the session, in tomorrow's digest, instead of a phone call in March from somebody who turned up to a class they are no longer on. *Both, disagreeing* — per COLUMN, because "these two rows differ" is not something anybody can act on and "Meals_Ordered says 2 and the tab says 4" is.<br><br>**Compared on a LIST of columns, not on all of them** (`LEDGER_VERIFY_COLUMNS`): a registration is who, on what session, in what state, with what meal. The two link cells are stamped onto every row from a registry by `69` and are a fact about the workbook; `Admin_Notes` is free text every stamper appends its own sentence to, and comparing it would report every cancellation in the building as a disagreement — which is a bucket nobody would read twice. Values are worn down before they are compared (`ledgerVerifyValue_`), because reporting `2` against `'2'` is how a report of real faults becomes a report nobody opens. A `Superseded` row is out of scope on both sides, since the fold never writes one out and counting it would report every resubmission as a loss.<br><br>**Read-only, never throws, and a clean run files NOTHING.** It runs last in `98`'s tail, after the sync has done everything that matters, and a verifier that took the sync down with it would be a diagnostic causing the fault it reports; a side that could not be read says so rather than printing an empty bucket, because "nothing to report" and "nothing looked at" are different answers (`32`'s and `51`'s rule). An hourly "the ledger agrees with the tab" is twenty-four lines a day saying nothing happened, which is how a digest stops being read. Also `showLedgerVerificationReport()`, the same answer on demand under 🔧 Admin ▸ 📄 Reports, for the person standing in front of a roster that is missing a name. `tests/ledger_verifier.test.js` pins the three buckets apart, the per-column finding, the clean run's silence, the superseded exclusion, and that an unreadable side is never drawn as a clean bill of health. |

### The ledger's one-time backfill (99o)

Numbered after `99n` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own four constants stand alone, its schema
is `HEADERS.All_Registrants` in `03` like every other tab's, and everything it
reaches for — the ledger (`99k`), `runSlicedJob` (`75`), `getSectionZones` /
`getHeaderMapAt` (`34`, `07`), `ensureSheetColumns` (`13`), `withScriptLock`
(`06`), `spoolOfficeNote` (`88`) — it reads at CALL time or through a hoisted
function declaration. One earlier file names it: `16`, on Admin ▸ One-Time Jobs.

| File | | What is in it |
|---|--:|---|
| `99o_ledger_backfill.gs` | 423 | **Everything on `All_Registrants` today predates the ledger.** Phase 2 put an append in front of every writer, so a registration that CHANGES is on the record from the day it shipped — but a row nobody has touched since is not, and that is most of the tab. Until this has run, the verifier's first bucket (`99n`) is the whole workbook and therefore says nothing. `backfillLedgerFromTab()` writes one `registered` entry per row, `Source: 'migration'`, and puts the fold key back ONTO the row, which is most of why that column exists. A row that is `Cancelled`, `Waitlisted` or `Superseded` gets its `registered` entry PLUS the one entry that says so (`ledgerEntriesForExistingRow`, `99k`), because the fold's `registered` creates an Active registration by construction and a replay built from `registered` alone would put every cancelled seat in the building back.<br><br>**Idempotency is RESOLVE-BEFORE-MINT, not "skip a row that already has an id"** — the design's first rule, corrected on 2026-09-23, and the correction is the whole of what makes this safe. That rule keys the idempotency on the one field that is blank on every row the job exists to process: the job is sliced, Apps Script kills an execution at its ceiling with no exception and no `finally`, and a slice that died between the append and the column write would leave a blank row for the next slice to mint a SECOND id and a SECOND `registered` entry against. The fold keys state on `Registration_ID`, so that is two live states for one person — two seats against a capacity and two meals against a catering count, the exact duplication `85` exists to clean up, manufactured by the migration meant to be invisible. So every row is resolved against the ledger first and minted only on a miss, and the id goes onto the row BEFORE the entries are flushed: a row with an id and no entry is the safe failure (the tab is authoritative until phase 4 and the verifier names the row), an entry no row claims is not.<br><br>**And the resolution it uses sees DEAD registrations**, which is the one place `resolveRegistrationId()` deliberately cannot help: a cancelled row's id is dead the moment it is minted, so the live index would never find it again and every slice would record it afresh. `ledgerRegistrationIdsByKey()` (`99k`) answers the other question, and claims each key ONCE — two tab rows sharing one key are a duplicate registration for `85`'s dialog to resolve, and the backfill reproduces the tab rather than quietly collapsing them into one.<br><br>**The ids go back as a column write, never a re-render.** Calling `renderRegistrantsSheet()` per slice to persist them would be the read-self / `clear()` / rewrite operation this whole design distrusts, run repeatedly over the entire tab and straight into `99j`'s shrink guard — so it is one `setValues()` over one column of one section zone, on `96`'s pattern, with the zones re-read at the top of every slice and never carried across executions. **`Occurred_At` is the session's own date**, which is the design's "otherwise" clause and the only one a row can answer: `Form_Source` holds a `=HYPERLINK()` naming one RESPONSE, not a time, and recovering the timestamp would mean opening every form this workbook ever made, once per row, inside a job holding the desk lock. It is honest about being the ORDER rather than the minute.<br><br>**The column is this commit's migration, and it needed almost nothing**: `buildHeaderProjectionFromRow()` (`34`) already projects a canonical column the sheet does not have to `-1` and reads it back blank, which is what makes adding a column to a `HEADERS` array safe on a tab holding data, and the next full render writes it out with everything else. `ensureLedgerIdColumn_()` adds only the header cell, so a targeted write has somewhere to land before then. Hidden via `REGISTRANT_HIDDEN_COLUMNS` (`39`) the way `PROGRAM_MONTH_HIDDEN_COLUMNS` hides `Room`. `tests/ledger_backfill.test.js` pins the resolve-before-mint rule on a blank row and on a dead one, the two entries a dead row gets, that a superseded row is never written back out as live, the duplicate-key rule, and that `Registration_ID` is never a column the verifier compares. |

### Two rows, one session (99l)

Numbered `99l` rather than `99f`, which this was written under: the volunteers'
own hours page took that prefix on the branch while this was in flight, and the
registration ledger then took `99k` — renumbering an EXISTING file is the one
edit this project cannot make, so the newcomer moved twice, the same way
`80`–`82` did. Safe there: behavior only, it declares no constant anything else derives
from, its schema is `HEADERS.All_Program_Sessions` in `03` like every other
tab's, and everything it reaches for (`getSectionedRows` /
`getSectionedRowValues`, `renderProgramDashboard`,
`recomputeEventRegistryCounts`, `isWaitlistOnlyColumnValue`) it reads at CALL
time or through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `99l_duplicate_session_rows.gs` | 234 | **An `Event_ID` was never unique, and nothing said so.** It is `calendarId \| cleanTitle \| dateKey` and nothing else, and EVERYTHING downstream treats it as the identity of a session: the registrant rows join on it, the tombstones match on it, capacity is recounted per it, Quick Mark and both door pages look a session up by it, a form response is resolved back through it. But `buildEventGroups()` (`24`) pushed every calendar event onto its group's session list unconditionally and `writeEventRegistryRows()` (`26`) mapped that list one-to-one into rows — so two events on ONE calendar, on ONE day, under ONE clean title (an appointment program typed as a 9–12 block and a 1–4 block, a recurring event with a one-off copy beside it, an event somebody duplicated instead of moved) became TWO rows carrying ONE `Event_ID`. **The calendar was right and the form was right** — a form's date list is keyed by date, so the twins collapse there by themselves — which is exactly why this went unseen: the duplication was visible only on the tab everybody reads, and the second row's counts were stale forever, because `recomputeCountsForZone()` writes the answer for an `Event_ID` once.<br><br>**The fix is in `26`, not here**: `collapseSessionsByEventId_()`, one row per `Event_ID` however many events fall on the date. It keeps the EARLIEST event and invents nothing from the others — in particular it does NOT widen the span to cover them, which for an appointment program would manufacture slots over a lunch break nobody is working through. The one thing merged is `[Waitlist Only]`, which is a statement about the DATE (`WAITLIST_ONLY_TAG`), so either event carrying it closes the date — the same "a missing tag is an omission, not a contradiction" reading `buildEventGroups()` takes for every program flag. It is collapsed at ROW-WRITING time rather than at group build, because the group's `events` list is also what `backInjectCalendarDescriptions()` writes the registration link onto: a second event on one day is still a real event and still needs its link, even though it must not become a second row. **And it is never silent** — every collision is logged and filed for the office digest, pointing at "⏱️ Merge Half-Hour Blocks" (`56`), because collapsing without saying so trades a visible duplicate for an invisible one.<br><br>This file is the other half: the rows written before that rule existed. `findDuplicateSessionRows` is the pure split; `mergeDuplicateSessionRows_` merges ADDITIVELY — a row written in a run that could not open the form has empty link cells and its twin does not, and taking either one whole would throw away what the other knows (the same reading as `mergeRegistrantRow` in `85` and `mergeMemberRollRows` in `79`). The derived counts are deliberately NOT carried across; they are recomputed from the registrant rows the moment the sweep lands. A BLANK `Event_ID` is never an identity two rows can share. `reportDuplicateSessionRows` is read-only and ungated, under Admin ▸ Reports beside the leftover-calendar report; `removeDuplicateSessionRows` is gated, beside its sibling under Destructive. **It is the one removal in this workbook that moves nobody to Triage** (`84` does, and must — there the session itself is going): the surviving row keeps the same `Event_ID`, so every registration attached to that session is still attached to it afterwards. `tests/duplicate_session_rows.test.js` pins the earliest-wins rule, the un-widened span, the OR-ed tick in both directions, that two buildings on one day are two sessions, the additive merge, the uncarried counts and the blank-id rule. |

### Closing a run of dates at once (99m)

Numbered after `99l` for the usual reason, and moved from `99g` with it. Safe there: behavior plus one
constant nothing else derives from, its schema is `HEADERS.All_Program_Sessions`
in `03`, and everything it calls — `loadSessionGrid` / `sessionGridColumn` /
`markSessionGridColumn` / `flushSessionGrid` (`96`), `getSessionFlagByColumn`
(`02`), `recordPendingProgramFlag` (`18`), `withScriptLock` (`06`) — is a
hoisted function.

| File | | What is in it |
|---|--:|---|
| `99m_bulk_waitlist_only.gs` | 420 | **`Waitlist_Only` one cell at a time, on a tab of several hundred rows.** It is the one tick on the session table that means something about a DATE rather than a program (`WAITLIST_ONLY_TAG`, `SESSION_FLAG_COLUMNS`), and the only way to set it was to find the program, find the date, tick, repeat — the wrong shape for what people actually say ("the Tuesday class is full until the new term", "close the whole of December while the hall is painted"). `showBulkWaitlistOnlyDialog` picks the PROGRAM first, which is also what makes it safe: every date in the list is then one program's, and a tick can never land on the Bingo sitting beside it in date order. `listWaitlistProgramSessions` groups on `Calendar_Source \| Clean_Title` — the same boundary `spreadFlagToSiblingRows()` and `reconcileProgramFlagColumns()` draw, so two locations running an unlinked "Chair Yoga" stay two programs — and offers only UPCOMING dates, with from/to boxes beside them for a long-running program. A row with no `Calendar_Source` (a lunch or hand-typed row) is never offered: there is no event to stamp the tag onto, so closing it would be half an answer.<br><br>**It goes through the same two steps an edit does, deliberately.** A tick is not a cell — it is a cell AND a queue entry the installable trigger turns into a `[Waitlist Only]` tag in the calendar event's description (`handleWaitlistOnlyEdit`, `18`). Writing only the cell would leave the sheet and the calendar disagreeing, and the next `syncCalendars()` recomputes this column FROM the calendar, so a bulk tick that skipped the queue would be quietly undone within the hour. `recordPendingProgramFlag()` carries the row's dateKey for exactly this reason: one program, several dates, each queued on its own.<br><br>**And it unticks.** The list arrives with the already-closed dates already ticked and the dialog sends the WHOLE picture, so "we are reopening the 14th but not the 21st" is one action rather than two. A date whose tick did not change is neither written nor queued — an hourly sync should not be asked to re-stamp a calendar that already agrees. The program key is re-checked against every row rather than trusted, because a rename between the dialog opening and the button being pressed makes that row a different program. **Nobody already registered is moved**, here as anywhere else this tick is set: it decides where the NEXT person goes, and promoting or waitlisting somebody who has a place is `71`'s business. `tests/bulk_waitlist_only.test.js` pins the two-building split, the past-date exclusion, the seeded ticks, that only changed dates are queued and each with its own date, the untick, the refusal on a renamed row, and that a program titled `</script>` cannot end the page. |

### A pasted list onto one program (99q)

Numbered after `99p` for the usual reason — never renumber, and this landed
last. Safe there: behavior only, its own constants stand alone, and everything
it reaches for (`collectKnownProgramChoices` / `applyQuickMarkLocked` in `38`,
`readMemberRollRows` / `splitPersonName` in `79`, `canonicalMemberName` in
`77`, `parseCsvText` in `11`) it reads at CALL time or through a hoisted
function declaration.

| File | | What is in it |
|---|--:|---|
| `99q_bulk_manual_registrants.gs` | 544 | **Add Registrants in Bulk**, on the menu beside Add Members to the Roll. Pick a PROGRAM (building × title, upcoming dates through the end of next month; appointment and lunch-only rows are not offered), paste a list, and REVIEW before anything is written: each row is matched against `Member_Roll` as `exact` (used as-is), `contact` (same phone/email, pre-selected when there is one candidate), `similar` (same surname + initial — offered, never chosen, because that is also a married couple) or `new`. How often is set for the list and per row: next date, ticked dates, every date, or a standing place (`Club_Members`). **Every write is Quick Mark's Register** (`applyQuickMarkLocked`), so duplicates, capacity, the walk-in roll entry and the wording are the desk's own; the lock is taken per person and the run stops at `BULK_REGISTRANTS_BUDGET_MS` and names who is left. `tests/bulk_manual_registrants.test.js` pins the paste, the four match kinds, the grouping and the date choice. |

### Putting back what an older copy still has (99p)

Numbered after `99o` for the usual reason — it was written as `99n`, and the ledger's verifier and backfill took `99n`/`99o` first — never renumber, and this landed
last. Safe there: behavior only, its three constants stand alone, its schema is
`HEADERS.All_Registrants` / `HEADERS.Club_Members` in `03`, and everything it
reaches for — the tombstones (`28`), the duplicate key (`85`), the club roster
(`41`), the every-date registry (`06`), both renders and the two recounts — it
reads at CALL time or through a hoisted function declaration.

| File | | What is in it |
|---|--:|---|
| `99p_restore_from_copy.gs` | 619 | **The recovery path `99j` says exists, made usable.** For anything lost before the ledger (`99k`) was being written to, the only second copy of a registration is a copy of the whole workbook made from Google's version history. `showRestoreRegistrantsFromCopyDialog` (Rosters & Sharing, beside Remove Marked Registrants — its opposite) takes that copy's link, reads its `All_Registrants` and `Club_Members` beside the live ones, and lists who the copy has that the workbook does not — **one tick per person per program, grouped by likely cause** (`classifyCopyRegistrants`, pure): a form response behind the sync clock, a desk/door row nothing can rebuild, a standing-list booking, an every-date registration still on file, one submission on several dates that looks recurring, or no recorded cause. **What was removed on purpose is never offered**, only counted: still on the session under any spelling or status (`duplicateRegistrationKey`), cancelled or superseded in the copy, tombstoned, on `Deleted_Event_Triage`, or a club booking whose membership is now inactive. A row whose session no longer exists is counted and named but not offered. A club booking whose MEMBERSHIP is gone is flagged, and the copy's active memberships the live tab has no row for are offered alongside — without them the next date is never booked. The every-date registry is only READ: whether somebody asked for every date is on neither spreadsheet. A restored row goes back verbatim (`buildRestoredRegistrantRow`) with `Manual_Override` protecting it, an `Admin_Notes` stamp naming the copy, and the derived link cells and a formula `Event_Time` blanked for the render to rebuild — and the LEDGER FIRST: `ledgerEntriesForExistingRow` under `LEDGER_SOURCES.RESTORE`, flushed before either tab is written and refusing the whole restore if it did not land, with a freshly minted `Registration_ID` (a copy's own is not trusted). The dialog sends only ids; `restoreRegistrantsFromCopy` re-reads both spreadsheets under the workbook lock and restores only what is still missing. The copy is read with `readAllSectionedRows`, not `getSectionedRows`, whose memo is keyed on a tab NAME the copy shares. `tests/restore_from_copy.test.js` pins every hidden case, every cause, the collapse of the copy's own duplicates and the restored row's shape. |

### Which menu items anybody presses (99r)

Behavior only, numbered last for the usual reason. Its two constants stand alone and nothing runs at load. The wrappers are hoisted declarations, not generated onto `globalThis`, so load order cannot lose one.

| File | | What is in it |
|---|--:|---|
| `99r_menu_usage.gs` | 190 | **One thin `menu_<action>` wrapper per menu item** (`function menu_x() { recordMenuUsage_('x'); return x(); }`). Each click adds one to a count and stamps a last-used time in ONE versioned property (`MENU_USAGE_V1`, keyed by action so a relabel keeps its history). `recordMenuUsage_` is guarded whole and runs BEFORE the action, so tracking can never cost the click, and it takes no lock. 🔧 Admin ▸ 📄 Reports ▸ **Menu Usage** (`showMenuUsageReport`, ungated) lists items most-pressed first with last-used dates, and below them the items never pressed. Labels come from running `buildAppMenu` against a recording stub, so they cannot drift from the menu. **Reset Menu Usage Counts** asks first. `tests/menu_usage.test.js` pins that every item resolves to a wrapper around a real action, the increments, the sort, and that a broken or garbage property still runs the action. |
